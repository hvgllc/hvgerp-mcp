---
review_verdict: APPROVE
plan_id: 007
reviewed_commit: 1aae3db9532ab6af2d332849e20c374d75984c6b
completed_commit: 0cf6a69463fef96f95512d36dda92ec2ad286f22
reviewed_evidence_blob: 9feb2a5c7c1b59a76a4bfd40a9880a6851cb8b96
completed_evidence_blob: 9feb2a5c7c1b59a76a4bfd40a9880a6851cb8b96
---

Metadata binding được bổ sung sau review PR25: reviewed_commit nay chỉ HEAD cuối
đã được Codex xác nhận sạch và CI thật như phần lịch sử bên dưới; commit review
độc lập gốc vẫn là `0ec1bebef82ca01814253e6b1dc355c6f9d4e823`. completed_commit
là merge thật đã xác minh, không phải HEAD backlog hiện tại. Hai evidence blob
ID lấy từ Git tại đúng path của kế hoạch này trong hai commit lịch sử, không
hash báo cáo mới để tự tạo bằng chứng. Validator so toàn bộ scope và artifact
giữa hai revision; đây là gate nhất quán, không xác thực danh tính reviewer hoặc
CI offline.

# Bằng chứng thực thi 007

Ngày: 2026-09-05. Base: fbe9528. Executor làm việc trên branch
advisor/007-browser-typecheck-gate, trong worktree riêng. Gate nguồn và browser
QA đã có bằng chứng; reviewer độc lập APPROVE sau vòng sửa 1. Vẫn cần
CI/preflight với JSR thật trên HEAD tích hợp cuối cùng trước khi merge.

## Baseline đỏ đúng nguyên nhân

- Git status ban đầu sạch. Diff từ d2c5305 tới base trên các file trong phạm vi
  rỗng, không có source drift cần giải thích.
- Cài UI bằng npm ci --offline --no-audit --no-fund, đúng package-lock có sẵn;
  không sửa dependency/version/lockfile UI.
- Chạy src/ui/node_modules/.bin/tsc --noEmit -p src/ui/tsconfig.json: exit 2,
  105 diagnostics. Mười diagnostics production nằm tại ChartViewer (4 vị trí,
  bao gồm kiểu callback Bar, Pie label/click, tooltip nhận undefined) và hai
  detail panel (3 unknown JSX mỗi file). 95 diagnostics còn lại do root resource
  helper kéo runtime Deno/Node, Deno tests trong browser project, hoặc cấu hình
  chưa cho import .ts.
- Output nguyên bản: [baseline-tsc.txt](007/baseline-tsc.txt).

## Thay đổi và coverage

- Browser project include toàn bộ *-viewer/src TS/TSX, shared TS/TSX và testing
  TS. Giữ strict và skipLibCheck nguyên trạng; thêm allowImportingTsExtensions
  cùng noEmit. Không xóa tests hoặc bỏ production module để né lỗi.
- Recharts dùng inference callback đúng bản lock. Bar lấy label từ vị trí data
  đã truyền; Pie label/tooltip xử lý undefined. Hai detail panel chỉ render các
  trường văn bản khi giá trị thực là string.
- npm script typecheck được chạy sau npm ci, trước build trong cả workflow Test
  và release-check. Workflow vẫn chỉ workflow_dispatch; có thêm preflight không
  publish để CI kiểm với JSR thật.
- Host dùng AppBridge/PostMessageTransport thật, bảy viewer từ UI_VIEWERS,
  fixture local, trace và điều khiển phản hồi theo ID. Không gọi ERP hoặc đọc
  credential. Không sửa các lỗi khác mà scenario có thể phơi bày.
- --listFilesOnly exit 0, có đủ bảy main.tsx và toàn bộ shared module được
  include. Danh sách source thực tế:
  [browser-project-files.txt](007/browser-project-files.txt). Không có
  runtime.deno.ts, runtime.node.ts, Deno test hoặc resource loader. viewers.ts
  vẫn có mặt vì host import registry hằng số thuần, không import mod.ts hoặc
  viewer-resource-paths.ts.

## Lệnh đã chạy

Các lệnh Deno test/check local bên dưới dùng --config deno.nojsr.json
--sloppy-imports --frozen. Lint dùng config local. Format toàn worktree dùng
--ignore=.nojsr-vendor/ để không đụng vendor pristine.

| Gate                 | Lệnh                                                                          | Kết quả                                                                          |
| -------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| Browser              | npm --prefix src/ui run typecheck                                             | exit 0                                                                           |
| Coverage browser     | src/ui/node_modules/.bin/tsc --noEmit -p src/ui/tsconfig.json --listFilesOnly | exit 0, 7 entrypoint                                                             |
| Server               | deno check [flags local] mod.ts server.ts                                     | exit 0                                                                           |
| UI tests Deno        | deno test [flags local] --allow-all src/ui/                                   | exit 0, 45 passed, 0 failed                                                      |
| Hồi quy              | deno test [flags local] --allow-all src/                                      | exit 0, 830 passed, 0 failed, 4 ignored có sẵn                                   |
| Lint                 | deno lint --config deno.nojsr.json                                            | exit 0, 193 files                                                                |
| Format               | deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/             | exit 0 trước khi thêm evidence                                                   |
| UI build             | node build-all.mjs trong src/ui                                               | exit 0, đúng 7 viewer                                                            |
| Node build           | npm_config_offline=true bash scripts/build-node.sh                            | exit 0, framework 0.25.0                                                         |
| Nội dung npm         | npm_config_offline=true npm pack --dry-run --json trong dist-node/bin         | exit 0, đúng 10 file: bundle, README, package.json và 7 HTML; không testing/host |
| Diff                 | git diff --check                                                              | exit 0                                                                           |
| Preflight nguyên bản | deno task release:check                                                       | BLOCKED local, xem bên dưới                                                      |

## Giới hạn local và tính nguyên vẹn

Preflight nguyên bản đã chạy nhưng dừng ở deno fmt --check: vendor pristine có
src/observability/otel.ts chưa định dạng theo Deno hiện tại. Chưa chạy tới bước
JSR. Không sửa vendor hoặc bỏ gate trong preflight để làm kết quả xanh. CI không
có artifact local này và phải chạy lại toàn bộ preflight trên JSR thật.

Lần thử deno task đã thay workspace entries trong lockfile local bị bỏ qua bởi
Git. Đã phục hồi bằng apply_patch từ bản chuẩn, sau đó chạy check/test với
frozen thành công. SHA-256 cuối cùng của deno.lock:
f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a. diff -qr xác
nhận vendor byte-identical cache npm @casys/mcp-server/0.25.0. Config local khớp
deno.json ở tất cả trường ngoài imports, bao gồm publish exclude mới. Không
commit config/vendor/lock local hoặc build artifacts.

deno.json loại src/ui/testing/** khỏi publish. Kiểm npm pack dry-run xác nhận
host không có trong package Node. Chưa publish hoặc bump version.

## Browser QA và review

Parent đã đọc toàn bộ diff, tự kiểm browser và chạy lại gate sau khi merge main
c1e7485 vào nhánh 007, tại HEAD 552e69f. Browser typecheck, server check, lint
193 file, format 221 file, diff check, npm ci đúng lock, build đủ 7 UI, Node
bundle và node --check đều exit 0. Build hoàn tất rồi mới chạy full suite: 847
passed, 0 failed, 4 ignored. Không đổi source sau APPROVE độc lập. Chưa coi
preflight local nguyên bản hoặc CI là đã đạt; chờ workflow Test trên HEAD được
push. Bằng chứng browser và giới hạn ở [browser-qa.md](007/browser-qa.md).

### Vòng sửa 1 từ reviewer độc lập

QA tương tác phát hiện detail Kanban tự gọi erpnext_user_list sau khi tải doc.
Đã bổ sung fixture read-only riêng cho Kanban đúng listResult User của setup.ts:
doctype/count/returned/has_more/data với name/full_name/enabled, cùng metadata
Doclist. Không thêm mutation; mọi tool chưa được nhận diện khác vẫn trả lỗi.

Finding hợp lệ: fixture move success thiếu cardId/fromColumn/toColumn so với
KanbanMoveResult ở src/kanban/types.ts và task adapter. Đã bổ sung ba trường từ
card.id, card.columnId và column.id. result() tuần tự hóa payload ngay khi
responseFor nhận request, trước khi giữ deferred reply; fromColumn vì vậy không
bị thay bằng cột đích sau mutation. Trạng thái fixture vẫn chỉ cập nhật khi trả
success, không cập nhật khi reject hoặc cancel.

Sai khác có giải thích với kế hoạch: host import viewers.ts là registry hằng số
thuần để không tự tạo danh sách viewer thứ hai. Nó không kéo resource loader
hoặc runtime adapter vào project browser. Shared Kanban production không cần
sửa: allowImportingTsExtensions xử lý lỗi cấu hình của các import .ts hiện có.
Không thêm import server types vào host chỉ để kiểm fixture vì chuỗi type đó có
thể kéo môi trường server vào browser project.

Sau sửa đã chạy lại browser typecheck, lint, format và git diff --check, tất cả
exit 0. Reviewer độc lập APPROVE host có SHA-256
3ffaca5d659ca9b4f2ee413d07f5cffe72a983e1a4f817508c6132b7471c0691.

Kiểm thêm bằng Node VM với chính phần responseFor/result/failure trong host và
fixtures.ts thật: phiên bản trước sửa (bỏ đúng ba trường mới trong bộ nhớ) lỗi
assertion vì thiếu cardId; phiên bản hiện tại kiểm đủ cardId/fromColumn/toColumn
và serverCard.columnId đều đạt. Gọi responseFor chưa đổi trạng thái card. Sau
khi đổi trạng thái card trong VM, reply đã chụp vẫn giữ fromColumn ban đầu.
Output:

```text
RED: previous fixture fails required move identity contract
GREEN: complete move contract, no eager mutation, source column captured before deferred delivery
```

### Browser QA thực tế

Agent điều phối đã kiểm Chrome thật qua Browser plugin: cả bảy viewer, các biểu
đồ bar/horizontal-bar/pie/donut, hai detail panel, download CSV, các kịch bản
race/error và Kanban ở viewport 390x844. Chi tiết và giới hạn nằm trong
[browser-qa.md](007/browser-qa.md), DOM/trace gốc trong
[browser-qa.json](007/browser-qa.json), ảnh tại [browser/](007/browser/).

Host tái hiện được lỗi CSV hiện có thuộc 008 và board-race thuộc 016; không đánh
dấu các lỗi đó là đã sửa. Console có warning Recharts khi mount nhưng render và
tương tác sau layout đạt; không tuyên bố console hoàn toàn sạch. Guard undefined
được kiểm bằng kiểu và đọc nguồn, chưa có payload browser cố ý thiếu trường.

Host bind 127.0.0.1:5178. QA chỉ dùng fixture local với AppBridge và viewer
build thật, không thay bằng chứng ERPNext production hoặc gate JSR trên CI.

## CI và merge đã xác minh

Ghi chú chờ CI ở trên là lịch sử. Parent đã xác minh PR #27 MERGED lúc
2026-09-05T05:14:30Z tại `0cf6a69463fef96f95512d36dda92ec2ad286f22`.

- PR: https://github.com/hvgllc/hvgerp-mcp/pull/27
- HEAD đã kiểm: `1aae3db9532ab6af2d332849e20c374d75984c6b`.
- Codex clean_comment `5549564964` khớp HEAD, findings_error false và không có
  review thread.
- CI: https://github.com/hvgllc/hvgerp-mcp/actions/runs/33946387814
- Run terminal success trên đúng HEAD: 847 passed, 0 failed, 4 ignored; release
  preflight nguyên bản thành công với JSR @casys/mcp-server 0.25.0.
- Git local xác nhận tree HEAD và tree merge cùng
  `a2a3e573b8b2c56abff67eb320c7c7b144d6dab1`.
- Nhánh backlog nhận source bằng merge `dfedde0`, không sửa lại source. DONE chỉ
  dành cho 007; lỗi CSV 008 và board race 016 vẫn còn theo browser QA.

Chưa release, publish hoặc đổi production. Kết quả CI không phủ thay mọi gate
riêng của các kế hoạch chưa thực thi.
