---
review_verdict: APPROVE
definition_review_verdict: APPROVE
definition_commit: 7fcfc9da79bfd5344e1b5e75de0d08e96bf83c3e
definition_plan_blob: 0935025d88c4bb7c3a2d66544029b86c71720af4
definition_manifest_blob: 1b06619e8870abee5b0cc5e1b4e83c7b9f4301c4
plan_id: 025
reviewed_commit: 5f7a6e487c6a8775314e1d20ebbb15881e9e84a4
completed_commit: 03c22a31206188f0379a1b195efea10d36482343
reviewed_evidence_blob: a50b9322268e0c1fccd58300c400aace83d88909
completed_evidence_blob: a50b9322268e0c1fccd58300c400aace83d88909
---

# Bằng chứng thực thi thiết kế 025

- Ngày: 2026-09-05.
- Source đã đọc: `67896f3208caee923659f1900c399d87e99c403c`.
- Branch: `advisor/025-customer-360-design`.
- Phạm vi: chỉ [thiết kế Customer360](../../docs/design/customer-360.md) và bằng
  chứng này. Parent quản lý root plan và README/index.
- Đã đọc trọn plan 025 tại root, skill improve và closing-the-loop theo vai
  executor được parent dispatch trong worktree riêng. Không tự delegate hoặc tự
  ghi verdict review độc lập. Root index ghi 005/006 DONE; source base đã chứa
  hai thay đổi nhưng complete-read chỉ áp dụng analytics.

## Các bước đã thực hiện

1. Đọc source thực: sales.ts dòng 269/614; accounting.ts dòng 189; crm.ts dòng
   281; operations.ts dòng 561/656; resolve.ts dòng 77/141/225; list-result.ts;
   ui-refresh.ts dòng 53/89/193; DoclistViewer.tsx dòng 318/456;
   InlineDetailPanel.tsx dòng 190/224; InvoiceViewer.tsx dòng 254/282/736;
   frappe-client.ts dòng 948; analytics-context.ts dòng 350/420 và
   analytics-pagination.ts dòng 42. Query-map và giới hạn ghi trong Evidence của
   thiết kế, tất cả citation tại source SHA trên.
2. Phân biệt A composition/sendMessage với B tool/viewer mới. A chỉ được đề nghị
   cho khảo sát có người kiểm tra; không hứa read-only cưỡng chế vì viewer hiện
   tại có Submit/Cancel. Không có MCP tool resolveCustomer độc lập.
3. Viết bốn query-map và trạng thái, company/date/currency/permission riêng;
   không dùng company_name của Contact như Customer link. Contacts deferred,
   không bịa query. Generic fields/filters là cơ chế có thật nhưng cấu hình đọc
   đề xuất vẫn cần xác minh schema/quyền ở ERP đích.
4. Trace trên giấy 14 ca đầu vào/kết quả. Không chạy handler, fixture, Browser
   hoặc ERP production trong đợt thiết kế này. Không gọi đó là tính năng đã hoạt
   động, không xác nhận nhu cầu người dùng.

## Lệnh và giới hạn xác minh

Kiểm trạng thái đầu worktree sạch và source SHA bằng `git status --short`,
`git rev-parse HEAD`;
`git diff --stat d2c5305..HEAD --
docs/design/customer-360.md` không có thay đổi
cũ cần bảo toàn ở file đích.

Gate tài liệu trước commit: kiểm bảy heading rubric đúng thứ tự, hai phương án,
bốn query-map/trạng thái, 14 ca Verification; kiểm relative links và không có
U+2014; `deno fmt --check docs/design/customer-360.md plans/evidence/025.md` và
`git diff --check`. Kết quả thực chạy được ghi ở phần chốt bên dưới.

Không cài dependency, không build, không sửa source/schema/fixtures/root/index,
không push/PR/merge. Không chạy full suite hoặc native JSR vì chỉ tạo Markdown;
không suy gate tài liệu thành runtime/CI pass. Review độc lập của parent là gate
còn chờ trước khi đánh dấu thiết kế hoàn tất trong backlog.

## Kết quả gate tài liệu

- `deno fmt` và `deno fmt --check` cho đúng hai file: đạt, 2 file.
- Probe Node chỉ đọc Markdown: đúng 7 heading rubric theo thứ tự, 4 query-map,
  bảng 4 trạng thái cho mỗi section, 14 ca Verification; không có ký tự U+2014.
  Hai phương án được đối chiếu bằng tự đọc bảng Alternatives, không suy nội dung
  từ một phép đếm keyword.
- Gate liên kết ban đầu dùng `existsSync`: 15/15 tồn tại trên filesystem macOS
  không phân biệt hoa thường, chưa đủ chứng minh tên đường dẫn đúng với Git. Sau
  finding của reviewer, đã tự chạy lại probe exact-case trước khi sửa: 13/15
  đạt, hai lỗi là `docs/roadmap.md` và
  `src/ui/doclist-viewer/src/DocListViewer.tsx`. Đã sửa thành `docs/ROADMAP.md`
  và `src/ui/doclist-viewer/src/DoclistViewer.tsx`. Probe Node chuẩn hóa
  relative target bằng `path.posix`, đối chiếu nguyên chuỗi với tập đường dẫn từ
  `git ls-files -z` cho cả thiết kế và evidence: sau sửa đạt 15/15 exact-case.
- Tự đọc lại thiết kế, đối chiếu citation và ranh giới current/proposed: không
  thêm company/date_to/offset giả vào dedicated API; không gán payment currency
  chưa chứng minh hoặc liên kết Contact bằng tên; không gọi viewer mutation là
  read-only.
- `git diff --check` và `git diff --cached --check`: đạt. Đã đọc toàn bộ staged
  diff và kiểm scope chỉ đúng hai file; không ghi APPROVE hoặc DONE thay
  reviewer độc lập.

## Review độc lập và kiểm tra của parent

Reviewer `/root/goal_execute_011` APPROVE tại HEAD
`96a365600daa7167775db538bbc55b0aec4055b3`, tree
`a41f565bbc6a9cb23c31c9a3e540bf39bf28f177`, evidence blob
`b2944630a6d03b4d9c3a51379571d1792d7c9666`. Reviewer đọc toàn bộ thiết kế,
evidence và source được viện dẫn; sau sửa hai citation không còn finding. Rubric
7 mục/2 phương án/4 query-map cùng trạng thái/14 ca trên giấy đạt; 15/15
relative link khớp exact case theo Git, format và diff check đạt.

Parent cũng đọc đầy đủ thiết kế/evidence, đối chiếu handlers và viewer liên
quan, tự chạy exact-case gate 15/15, format hai file và diff check đều đạt.
Contract chỉ đề xuất composition đọc có giới hạn; không tự thêm API, quyền,
pagination, liên kết Contact hoặc tổng tiền. Không chạy Browser/ERP hoặc gọi đây
là tính năng hoạt động. Các hạn chế chưa xác minh vẫn được giữ trong
Decision/Open questions. Cần CI và Codex trên HEAD cuối cùng trước merge PR tài
liệu; chưa ghi DONE thay cho các gate đó.

## CI, Codex và merge đã xác minh

- PR: https://github.com/hvgllc/hvgerp-mcp/pull/40
- HEAD được review: `5f7a6e487c6a8775314e1d20ebbb15881e9e84a4`.
- CI: https://github.com/hvgllc/hvgerp-mcp/actions/runs/33961219253 dùng JSR
  thật, 1314 test đạt ở cả hai lượt và release preflight không publish.
- Codex clean comment `5551236154`, ngày 2026-09-05 lúc 10:42:08 UTC, xác nhận
  đúng HEAD `5f7a6e487c`. Parent đã đọc toàn bộ phản hồi, không có finding.
- Tích hợp với main sau 013 tại `51a6dab29366127e776de1d553e7c4f67152d207`:
  reviewer độc lập và parent đều chạy 1338 test đạt, 0 thất bại, 4 bỏ qua.
  Reviewer kiểm server, lint, format, UI typecheck và build đủ 7 viewer; source
  ứng dụng bằng main, chỉ thêm hai tài liệu được duyệt.
- Merge commit: `03c22a31206188f0379a1b195efea10d36482343`, lúc 10:56:14 UTC.
  Sau fetch, tree `03878d9a41c9e6e52ee634ce74fb2b64da4d4dc0` bằng tree đã kiểm
  tích hợp; evidence blob đúng `a50b9322268e0c1fccd58300c400aace83d88909`.
- DONE chỉ cho thiết kế, không chứng minh nhu cầu người dùng, ERP đích hoặc tính
  năng Customer 360 đã triển khai. Supplemental definition approval của backlog
  là bước quản trị riêng, không tự suy từ các SHA ở đây.
