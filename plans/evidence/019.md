---
review_verdict: APPROVE
plan_id: 019
definition_review_verdict: APPROVE
definition_commit: b9d6d02a9692c3efff11836b97d8cfbc69da1ec7
definition_plan_blob: 5afbf32bc26fee61782ba7699a0ac79cca0eb28f
definition_manifest_blob: 2ff4089ea1fef9ae82699d021bc51be346747952
reviewed_commit: 78b0358a4ad77dfc441238810b572ef254da3e8f
completed_commit: 861fb16500b7b1fd4eb85a3da81942da24be8c84
reviewed_evidence_blob: 31499e277d0300a1b9f3909c83c9562876433e2f
completed_evidence_blob: 31499e277d0300a1b9f3909c83c9562876433e2f
---

# Bằng chứng thực thi 019

## Phạm vi và mốc source

- Base: `a63503efb6692c835934fcfe83b70dea2cd96522`, đã tích hợp kế hoạch 018
  và 012. Source commit: `e60c1b1bcfea4d47f525acaeac66fd09e47e0467`, nhánh
  `advisor/019-cache-write-value-isolation`.
- Đã đọc đầy đủ AGENTS, hướng dẫn improve execute và tài liệu workaround JSR ở
  workspace gốc. `git status --short` ban đầu sạch; diff chưa commit của ba file
  source trong phạm vi rỗng. Diff từ `d2c5305` được đối chiếu với thay đổi đã
  merge: giới hạn/sweep/FIFO của 018 và regression client của 003/004/010/012.
- Chỉ sửa `src/cache/memory.ts`, `src/cache/memory_test.ts`,
  `src/api/frappe-client_test.ts` và tài liệu này. Parent quản trị trạng thái kế
  hoạch; executor không tự duyệt, push hoặc merge.
- Các điểm ghi MemoryCache production đã tìm thấy là dữ liệu JSON từ
  FrappeClient và boolean của negative cache resolve. Không thấy consumer hiện
  có chủ ý ghi function; không suy rộng kết luận này sang plugin bên ngoài.

## Bước 1: Red trước sửa source

```bash
git status --short
git diff --stat d2c5305..HEAD -- src/cache/memory.ts src/cache/memory_test.ts src/api/frappe-client_test.ts
git diff -- src/cache/memory.ts src/cache/memory_test.ts src/api/frappe-client_test.ts
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/cache/ src/api/frappe-client_test.ts
```

Baseline đạt **166 passed, 0 failed**. Sau khi thêm 13 test, chạy cùng lệnh trên
source chưa sửa cho kết quả **167 passed, 12 failed**, exit 1. Hai test client
get/list và hai test cache thấy dữ liệu bị sửa qua alias; tám test clone error
thấy giá trị không clone được vẫn được lưu. Control TTL không dương đạt.

Lần thử trước đó bị lỗi TypeScript vì giá trị trả từ `assertThrows` có kiểu
unknown; đã thêm kiểm tra `instanceof DOMException` trong test và chạy lại. Lỗi
typing đó không được tính là red hành vi.

## Bước 2: Snapshot khi ghi

- TTL dương: gọi `structuredClone(value)` trước khi xóa entry sống cùng key hoặc
  thu hồi entry sống khác vì cap. Lỗi clone truyền nguyên ra caller.
- TTL bằng 0 hoặc âm vẫn xóa key và không cần clone giá trị. Sweep entry đã hết
  hạn vẫn diễn ra trước đó như 018; cam kết bảo toàn khi clone lỗi áp dụng cho
  entry còn sống, không giữ lại entry đã hết hạn.
- Giữ clone-on-read, thời hạn TTL, FIFO theo lần ghi, cap, Cache interface và
  source FrappeClient/generation của 012.
- Tổng cộng thêm 15 test: nested object/array và sửa kết quả đọc; Date, Map,
  Set, typed array, undefined và cycle; tám tổ hợp cap 1/2, key mới/cũ, function
  trực tiếp/lồng nhau kiểm tra clone error giữ value/expiry/FIFO; TTL 0/âm với
  giá trị không clone được; get/list miss không làm hỏng lần đọc cache sau;
  get/list đang bay qua mutation và cap-1 eviction không ghi lại snapshot cũ
  hoặc đẩy dữ liệu mới không liên quan ra khỏi cache.
- Hai regression đang bay dùng deferred promise để chốt thứ tự, không sleep. Các
  control cache hit xác nhận không có network request dư thừa.

## Bước 3: Green và hồi quy

```bash
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/cache/ src/api/frappe-client_test.ts src/api/caller-client_test.ts src/runtime-boundary_test.ts
deno check --config deno.nojsr.json --sloppy-imports --frozen mod.ts server.ts
deno lint --config deno.nojsr.json
deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/
git diff --check
npm ci --offline --no-audit --no-fund --prefix src/ui
npm --prefix src/ui run typecheck
node src/ui/build-all.mjs
npm_config_offline=true bash scripts/build-node.sh
node --check dist-node/bin/hvgerp-mcp.mjs
```

- Focused: **208 passed, 0 failed**, bao gồm cache/generation, client, caller và
  runtime boundary. Không xóa hoặc giảm regression cũ.
- Server check, lint, format và diff check: exit 0. Lint kiểm tra 200 file;
  format trước thêm tài liệu này kiểm tra 237 file.
- UI cài đúng lock offline, typecheck exit 0, build báo đủ **7 viewer**. Node
  build và syntax check exit 0.

Trong thư mục `dist-node/bin`, chạy:

```bash
npm pack --dry-run --json --offline
```

Exit 0, danh sách package có 10 file: bundle, package manifest, README và 7 HTML
viewer. Phiên bản vẫn là 3.4.0; không publish hay tạo release.

Sau khi UI và Node build kết thúc mới chạy suite đầy đủ:

```bash
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/
git diff --check
```

Kết quả: **1017 passed, 0 failed, 4 ignored**, exit 0. Các ca ignored là trạng
thái sẵn có, không phát sinh bằng cách giảm kiểm thử trong revision này. Đã đọc
toàn bộ diff source và test, xác nhận phạm vi đúng kế hoạch.

## Môi trường, chứng cứ và giới hạn

- Deno 2.9.5, Node v26.7.0, macOS arm64. Không tuyên bố đã chạy Node 20/22.
- Local config/vendor lấy từ worktree 012. Đã dùng deep equality kiểm tra mọi
  trường ngoài `imports` của `deno.nojsr.json` khớp `deno.json` và `diff -qr`
  vendor với npm cache `@casys/mcp-server` 0.25.0 nguyên bản: exit 0.
- Lock local giữ SHA-256
  `f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`; test/check
  đều dùng `--frozen`. Config, vendor, lock và build output là các hiện vật bị
  Git bỏ qua. Không nâng dependency hoặc thay config được theo dõi.
- Log tạm trong thư mục điều phối worktree: `019-baseline.log`, `019-red.log`,
  `019-green.log`, `019-full.log`, `019-ui-install.log`, `019-ui-build.log`,
  `019-node-build.log`. Đây là log local, không phải artifact CI lưu lâu dài.
- Các gate Deno dùng workaround đã được cho phép, không phải bằng chứng resolve
  JSR gốc. CI với JSR thật và review độc lập/Codex thuộc parent trước merge.
- Không gọi ERPNext thật, không dùng dữ liệu production hoặc credential. Chưa
  benchmark chi phí clone-on-write, không tuyên bố nhanh hơn hoặc không có chi
  phí bộ nhớ/CPU bổ sung. Backend cache mới vẫn cần tách giá trị ở cả đầu đọc và
  đầu ghi.

## Review độc lập và xác minh của parent

Reviewer độc lập `/root/review_005` APPROVE HEAD
`7ca35999fd573c02682fa3fcf54c125a2141c36e`, source
`e60c1b1bcfea4d47f525acaeac66fd09e47e0467`, không có finding. Reviewer tự đọc kế
hoạch, toàn bộ diff và evidence; chạy 208 test trọng tâm, full suite 1017
passed, 0 failed, 4 ignored, check, lint 200 file, format 238 file và diff
check. Không nhận kết quả red do executor báo là một lượt red của reviewer.

Parent tự đọc toàn bộ diff source/test và evidence, xác nhận phạm vi bốn file.
Parent chạy lại server check, lint 200 file, format 238 file, UI typecheck,
build đủ 7 viewer, Node build, Node syntax và full suite: 1017 passed, 0 failed,
4 ignored, exit 0. Pack dry-run offline từ dist-node/bin có đúng 10 file với 7
HTML viewer, không có testing host. Các lệnh dùng frozen workaround và Node
26.7.0 như phần trên, không được ghi thành kiểm chứng JSR gốc hoặc Node 20/22.

Nguồn đã được duyệt local. CI trên HEAD sẽ push, review Codex và merge thật vẫn
là các gate còn lại; kế hoạch chưa DONE.

## Merge và kiểm tích hợp

PR https://github.com/hvgllc/hvgerp-mcp/pull/36 đã merge lúc
2026-09-05T08:57:27Z, commit `861fb16500b7b1fd4eb85a3da81942da24be8c84`. CI
https://github.com/hvgllc/hvgerp-mcp/actions/runs/33955497233 đạt trên HEAD
`78b0358a4ad77dfc441238810b572ef254da3e8f`, dùng JSR thật; suite và release
preflight đạt 1017 test, không publish. Codex comment
https://github.com/hvgllc/hvgerp-mcp/pull/36#issuecomment-5550618566 xác nhận
không có vấn đề lớn, Reviewed commit `78b0358a4a`; parent đọc nguyên comment và
fetch helper xác nhận clean_comment true, findings_error false.

Reviewer độc lập kiểm cây kết hợp PR với main1751 ở detached commit
`47b68696e3cbc8d77d119f880c208d06d6cc62c3`: full suite 1103 passed, 0 failed, 4
ignored; server check, lint 201 file, format 240 file, UI typecheck/build đủ 7
viewer, Node build/syntax, pack 10 file/7 HTML và diff check đều đạt. Parent đọc
delta kết hợp và tự chạy full suite 1103 passed, 0 failed, 4 ignored.
Cache/client giữ source PR36, shim giữ source main011 đã review.

Parent merge bằng match-head-commit, đọc lại trạng thái MERGED và fetch main.
Tree GitHub `4ea4227579fb94c8c68e7555cd2c1c8bce2e665f` trùng hoàn toàn tree đã
kiểm thử, git diff exit 0; report blob trước/sau merge cùng
`31499e277d0300a1b9f3909c83c9562876433e2f`. Các gate tích hợp local vẫn dùng
workaround frozen và Node26, không được nhận là kiểm JSR gốc hoặc Node20/22. Mục
019 DONE; không release, publish, bump version hoặc sửa production.
