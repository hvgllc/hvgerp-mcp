# Bằng chứng thực thi 017

Trạng thái: source và các gate local đạt, chờ Browser 15 ca và review độc lập.
Chưa phải DONE.

## Phạm vi và contract

- Base: `99b1fa319590e60730faabdb033a5b48a44e1862`.
- Branch: `advisor/017-viewer-initial-error-state`.
- Worktree ban đầu chỉ có plan017 untracked của parent. Không stage file đó.
- Đã đối chiếu drift từ d2c5305: ChartViewer nhận chỉnh kiểu từ 007; host và
  fixture nhận 007/008/009. Không đổi Doclist, Kanban, detail fixture hoặc CSV.
- Đối chiếu InvoiceData với sales invoice get trả full document; StockData với
  stock balance/listResult, giữ count null và count_error; analytics với chart
  thường, scatter, treemap, KPI và funnel. Không thay payload tool server.

Types viewer được chuyển nguyên trường sang shared/presentation.ts để validator
và renderer dùng cùng type. Không có type assertion để ép JSON tùy ý thành
payload, không thêm any. Validator kiểm cả phần tử mảng và cây treemap, enum,
giá trị số hữu hạn và refresh metadata. Giữ optional fields, số 0, mảng rỗng,
count null, nullable optional ERP fields, invoice raw hoặc bọc data. JSON null
và invoice data null là empty hợp lệ; object sai shape là lỗi.

consumeViewerResult là chuyển trạng thái thuần được cả initial result và refresh
gọi. isError được xử lý trước parse; thông điệp text được ưu tiên trước
structuredContent khi tool báo lỗi. Lỗi JSON/schema kết thúc loading, giữ
reference data và refreshRequest cũ. Chỉ hydrate thành công mới thay dữ liệu và
xóa lỗi. Không tạo effect để đồng bộ trạng thái dẫn xuất; giữ refs cho refresh
gate hiện có theo skill React.

Năm viewer dùng getErrorPresentation trước empty state. Khi có data, lỗi ở
inline và dữ liệu còn nguyên. Invoice không còn nút dismiss xóa lỗi trước khi
hydrate thành công. Funnel có stages rỗng vẫn giữ thông báo refresh error cạnh
empty state.

## Red/green

Đợt test đầu không chạy được vì test import fixture kéo alias UI vào Deno và
initial state bị khai type quá rộng. Đây là lỗi thiết lập test, không coi là
regression red. Đã dùng payload độc lập khớp contract và type initial chỉ chứa
null, không đổi import map hoặc bỏ typecheck.

Để kiểm lỗi cũ trước implementation, tách nguyên luồng consume hiện có vào
helper thuần: Invoice/Stock kiểm isError bằng extractToolResultText, ba viewer
còn lại parse trực tiếp; không kiểm schema, cập nhật refreshRequest trước khi
hydrate. Helper có implementation thật, không phải stub hoặc module thiếu.

```sh
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/ui/shared/presentation_test.ts
```

- Red: 3 passed, 23 failed, exit 1. Các assertion thất bại do bỏ isError, dùng
  structuredContent làm thông điệp lỗi, thiếu content giữ loading, schema sai
  vẫn hydrate, refresh metadata sai thay reference cũ hoặc null không thành
  empty hợp lệ.
- Green sau validator/state transition: 26 passed, 0 failed.
- Bổ sung empty refresh, invoice raw/null/optional nullable và enum array:
  presentation 29 test. Wiring 2 test kiểm branch order và việc initial/refresh
  thực sự dùng helper. Toàn bộ 31 test trọng tâm đạt trong full suite.
- Wiring source chỉ bổ sung cho test hành vi, không thay thế Browser render.

Một kiểm độc lập bằng Node import fixture thật và serializer state xác nhận cả 5
viewer: success fixture được nhận, malformed fixture bị từ chối sau success,
data và refreshRequest giữ cùng reference. Không dùng fixture fake upstream làm
bằng chứng ERPNext production.

## Gate local

Môi trường: Deno 2.9.5, Node 26.7.0. Dùng workaround theo tài liệu được cho
phép. Config/vendor/lock donor 011 là bản local ignored; không sửa manifest hoặc
dependency. UI npm ci offline cài đúng lock, 208 package.

| Lệnh                                                                             | Kết quả                                 |
| -------------------------------------------------------------------------------- | --------------------------------------- |
| `npm ci --offline --no-audit --no-fund --prefix src/ui`                          | Exit 0                                  |
| `npm --prefix src/ui run typecheck`                                              | Exit 0                                  |
| `node src/ui/build-all.mjs`                                                      | Exit 0, đủ 7 viewer                     |
| `npm_config_offline=true bash scripts/build-node.sh`                             | Exit 0, framework 0.25.0                |
| `node --check dist-node/bin/hvgerp-mcp.mjs`                                      | Exit 0                                  |
| `deno check --config deno.nojsr.json --sloppy-imports --frozen mod.ts server.ts` | Exit 0                                  |
| `deno lint --config deno.nojsr.json`                                             | Exit 0, 198 file                        |
| `deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/`  | Exit 0, 926 passed, 0 failed, 4 ignored |
| `git diff --check`                                                               | Exit 0                                  |

Không chạy full suite đồng thời UI build. deno.lock giữ SHA-256
`f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`.

Format lần đầu source đạt, toàn worktree vướng đúng một dòng dài trong plan017
untracked của parent. Parent đã tự format file đó; executor không stage plan.
Gate cuối dùng
`deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/`. Kết quả
cuối: exit 0, 234 file. Vendor diff với cache npm pristine 0.25.0 rỗng.

## Bàn giao Browser

Parent giữ Browser binding và quản lý host. Executor không mở Browser mới, không
dừng host đang có, không mở spreadsheet hoặc gọi ERPNext.

URL mẫu:
`http://127.0.0.1:5178/testing/host.html?viewer=invoice-viewer&scenario=initial-error`.
Lặp invoice-viewer, stock-viewer, chart-viewer, kpi-viewer, funnel-viewer:

- initial-error: thấy Local initial error, không báo empty.
- malformed-payload: thấy Invalid invoice/stock/chart/kpi/funnel payload, không
  blank page hoặc renderer exception. Host chỉ gửi payload sai, không lọc lỗi.
- refresh-error: nhận success đầu, bấm Refresh, thấy Local refresh error và giữ
  số liệu cũ.
- Với initial hoặc malformed, bấm Gửi lại dữ liệu thành công để kiểm phục hồi.

Host thêm đúng scenario malformed-payload và dispatch theo từng viewer; giữ
handshake input/result và các scenario cũ. Browser 15 ca, ảnh/trace, console và
giới hạn do parent bổ sung trước đánh dấu DONE. CI JSR gốc trên đúng HEAD vẫn là
gate bắt buộc trước merge.

Không release, publish, nâng dependency hoặc thay dữ liệu người dùng.
