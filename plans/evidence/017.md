# Bằng chứng thực thi 017

Trạng thái: source và các gate local đạt, chờ Browser 15 ca và review độc lập.
Chưa phải DONE.

## Phạm vi và contract

- Base: `99b1fa319590e60730faabdb033a5b48a44e1862`.
- Branch: `advisor/017-viewer-initial-error-state`.
- Worktree ban đầu chỉ có plan017 untracked của parent. Không stage file đó.
- Đã đối chiếu drift từ d2c5305: ChartViewer nhận chỉnh kiểu từ 007; host và
  fixture nhận 007/008/009. Không đổi Doclist, Kanban, detail fixture hoặc CSV.
- Đối chiếu InvoiceData với sales invoice get trả full document; StockData với
  stock balance/listResult, giữ count null và count_error; analytics với chart
  thường, scatter, treemap, KPI và funnel. Không thay payload tool server.

Types viewer được chuyển nguyên trường sang shared/presentation.ts để validator
và renderer dùng cùng type. Không có type assertion để ép JSON tùy ý thành
payload, không thêm any. Validator kiểm cả phần tử mảng và cây treemap, enum,
giá trị số hữu hạn và refresh metadata. Giữ optional fields, số 0, mảng rỗng,
count null, nullable optional ERP fields, invoice raw hoặc bọc data. JSON null
và invoice data null là empty hợp lệ; object sai shape là lỗi.

consumeViewerResult là chuyển trạng thái thuần được cả initial result và refresh
gọi. isError được xử lý trước parse; thông điệp text được ưu tiên trước
structuredContent khi tool báo lỗi. Lỗi JSON/schema kết thúc loading, giữ
reference data và refreshRequest cũ. Chỉ hydrate thành công mới thay dữ liệu và
xóa lỗi. Không tạo effect để đồng bộ trạng thái dẫn xuất; giữ refs cho refresh
gate hiện có theo skill React.

Năm viewer dùng getErrorPresentation trước empty state. Khi có data, lỗi ở
inline và dữ liệu còn nguyên. Invoice không còn nút dismiss xóa lỗi trước khi
hydrate thành công. Funnel có stages rỗng vẫn giữ thông báo refresh error cạnh
empty state.

## Red/green

Đợt test đầu không chạy được vì test import fixture kéo alias UI vào Deno và
initial state bị khai type quá rộng. Đây là lỗi thiết lập test, không coi là
regression red. Đã dùng payload độc lập khớp contract và type initial chỉ chứa
null, không đổi import map hoặc bỏ typecheck.

Để kiểm lỗi cũ trước implementation, tách nguyên luồng consume hiện có vào
helper thuần: Invoice/Stock kiểm isError bằng extractToolResultText, ba viewer
còn lại parse trực tiếp; không kiểm schema, cập nhật refreshRequest trước khi
hydrate. Helper có implementation thật, không phải stub hoặc module thiếu.

```sh
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/ui/shared/presentation_test.ts
```

- Red: 3 passed, 23 failed, exit 1. Các assertion thất bại do bỏ isError, dùng
  structuredContent làm thông điệp lỗi, thiếu content giữ loading, schema sai
  vẫn hydrate, refresh metadata sai thay reference cũ hoặc null không thành
  empty hợp lệ.
- Green sau validator/state transition: 26 passed, 0 failed.
- Bổ sung empty refresh, invoice raw/null/optional nullable và enum array:
  presentation 29 test. Wiring 2 test kiểm branch order và việc initial/refresh
  thực sự dùng helper. Toàn bộ 31 test trọng tâm đạt trong full suite.
- Wiring source chỉ bổ sung cho test hành vi, không thay thế Browser render.

Một kiểm độc lập bằng Node import fixture thật và serializer state xác nhận cả 5
viewer: success fixture được nhận, malformed fixture bị từ chối sau success,
data và refreshRequest giữ cùng reference. Không dùng fixture fake upstream làm
bằng chứng ERPNext production.

## Gate local

Môi trường: Deno 2.9.5, Node 26.7.0. Dùng workaround theo tài liệu được cho
phép. Config/vendor/lock donor 011 là bản local ignored; không sửa manifest hoặc
dependency. UI npm ci offline cài đúng lock, 208 package.

| Lệnh                                                                             | Kết quả                                 |
| -------------------------------------------------------------------------------- | --------------------------------------- |
| `npm ci --offline --no-audit --no-fund --prefix src/ui`                          | Exit 0                                  |
| `npm --prefix src/ui run typecheck`                                              | Exit 0                                  |
| `node src/ui/build-all.mjs`                                                      | Exit 0, đủ 7 viewer                     |
| `npm_config_offline=true bash scripts/build-node.sh`                             | Exit 0, framework 0.25.0                |
| `node --check dist-node/bin/hvgerp-mcp.mjs`                                      | Exit 0                                  |
| `deno check --config deno.nojsr.json --sloppy-imports --frozen mod.ts server.ts` | Exit 0                                  |
| `deno lint --config deno.nojsr.json`                                             | Exit 0, 198 file                        |
| `deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/`  | Exit 0, 926 passed, 0 failed, 4 ignored |
| `git diff --check`                                                               | Exit 0                                  |

Không chạy full suite đồng thời UI build. deno.lock giữ SHA-256
`f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`.

Format lần đầu source đạt, toàn worktree vướng đúng một dòng dài trong plan017
untracked của parent. Parent đã tự format file đó; executor không stage plan.
Gate cuối dùng
`deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/`. Kết quả
cuối: exit 0, 234 file. Vendor diff với cache npm pristine 0.25.0 rỗng.

## Bàn giao Browser

Parent giữ Browser binding và quản lý host. Executor không mở Browser mới, không
dừng host đang có, không mở spreadsheet hoặc gọi ERPNext.

URL mẫu:
`http://127.0.0.1:5178/testing/host.html?viewer=invoice-viewer&scenario=initial-error`.
Lặp invoice-viewer, stock-viewer, chart-viewer, kpi-viewer, funnel-viewer:

- initial-error: thấy Local initial error, không báo empty.
- malformed-payload: thấy Invalid invoice/stock/chart/kpi/funnel payload, không
  blank page hoặc renderer exception. Host chỉ gửi payload sai, không lọc lỗi.
- refresh-error: nhận success đầu, bấm Refresh, thấy Local refresh error và giữ
  số liệu cũ.
- Với initial hoặc malformed, bấm Gửi lại dữ liệu thành công để kiểm phục hồi.

Host thêm đúng scenario malformed-payload và dispatch theo từng viewer; giữ
handshake input/result và các scenario cũ. Browser 15 ca, ảnh/trace, console và
giới hạn do parent bổ sung trước đánh dấu DONE. CI JSR gốc trên đúng HEAD vẫn là
gate bắt buộc trước merge.

Không release, publish, nâng dependency hoặc thay dữ liệu người dùng.

## Revision 1: hóa đơn dịch vụ không có item_code

Review độc lập của source 3a8fa2c phát hiện validator bắt item_code là string,
từ chối dòng hóa đơn dịch vụ hợp lệ. Executor đã tự đọc
[test chính thức ERPNext tại commit cố định](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/accounts/doctype/sales_invoice/test_sales_invoice.py#L1350):
ca test đặt item_code bằng None, insert và submit, rồi kiểm GL entries. Không
chạy test đó trên ERPNext production.

- Type InvoiceItem cho phép item_code thiếu/null; validator vẫn từ chối
  object/array. item_name nullable khớp kiểm tra optional hiện có.
- getInvoiceItemCode chỉ trả mã string không rỗng. Renderer giữ tên dòng dịch
  vụ; không gắn click, hover tương tác hoặc mở ItemDetailPanel khi không có mã.
  Dòng có mã vẫn mở chi tiết như trước. Refresh thay dòng đang mở bằng dịch vụ
  cũng không tiếp tục mount panel với mã rỗng.
- Regression initial và refresh kiểm riêng null/omitted, phục hồi từ lỗi,
  refreshRequest cũ và giá trị tiền cập nhật. Control object/array vẫn lỗi.
- Test red thực: 30 passed, 2 failed với Invalid invoice payload cho dòng null
  và omitted. Lần đầu trước đó dừng typecheck ở generic assertEquals vì type cũ
  bắt string; đã dùng assertEquals<unknown> để quan sát runtime, không dùng
  --no-check và không coi lỗi typecheck là red hành vi.
- Green cuối: presentation 33 test + wiring 2 test đạt. UI/server typecheck,
  lint, format, build đủ 7 viewer, Node bundle và node --check đều exit 0. Full
  suite sau build: 930 passed, 0 failed, 4 ignored. Lock SHA giữ nguyên.

Parent cho phép chạy build sau khi xác nhận không còn Browser QA đang chạy.
Executor không stop/restart Vite host. Fixture Invoice thêm hai dòng dịch vụ giá
0: Local service without item code (null) và Local service with omitted item
code (không có key). Giữ ITEM-LOCAL, các giá trị trước và grand/net total 30.
Browser cần kiểm hai tên hiển thị, click không mở panel/không gọi lookup Item,
và control ITEM-LOCAL vẫn mở panel. Browser đang chờ parent kết nối lại; không
tự tuyên bố đã kiểm render revision này hoặc tự duyệt finding.

## Review R1 và gate của parent

Reviewer độc lập APPROVE source `cb1ceac1ca6e8bc508efbbc3a22dab57ac90e3ab`, tự
chạy 35 test trọng tâm, UI typecheck và probe fixture thật. Parent đọc toàn bộ
delta R1 và xác minh nguồn upstream bằng GitHub Contents API ở đúng commit:
function bắt đầu dòng 1350, item_code None tại 1358, insert/submit tại
1359-1360. Đã sửa anchor dẫn nguồn, không đổi nội dung hợp đồng hoặc source
viewer.

Parent tự chạy UI/server typecheck, lint 198 file, format 234 file, build đủ 7
viewer, Node build và node --check, full suite **930 passed, 0 failed, 4
ignored**, diff check: đều exit 0. npm pack --dry-run trong dist-node/bin có 10
file, gồm đủ 7 HTML viewer, không có testing host. Bundle Node SHA256:
`77a531dc10ed1fc6cf57e38f0b8e49d94819bc3b2619e7b2b953cdb503527083`.

Browser QA chưa chạy trên R1. Kết nối Browser trả lỗi unavailable; retry vẫn
thất bại. Chẩn đoán read-only xác nhận Chrome đang chạy, extension enabled và
native-host manifest đúng. Theo hướng dẫn Browser, parent đã xin phép mở cửa sổ
Chrome mới và đang chờ phản hồi, không tự thay bằng trình điều khiển khác. Không
coi review source hoặc unit tests là bằng chứng render, recovery hay lookup khi
click. Chưa mở PR hoặc đánh dấu DONE.

## Browser QA hoàn tất trên revision 1

Sau bản ghi chờ kết nối ở trên, Browser phát hiện một phiên Chrome khác đã kết
nối. Parent nối lại phiên có sẵn, đọc đầy đủ API và tạo tab QA riêng; không mở
cửa sổ Chrome mới. Đã rút câu hỏi xin phép mở cửa sổ. Không dùng trình điều
khiển thay thế, không đọc state ẩn của ứng dụng.

Ngày 2026-09-05, Chrome extension, desktop 1920x816, host AppBridge local tại
port 5178 phục vụ bundle đã build từ source
`cb1ceac1ca6e8bc508efbbc3a22dab57ac90e3ab`. Parent thao tác bằng UI thực, đọc
DOM và xem trực tiếp ảnh. Đã đạt 15 ca chính: cả 5 viewer đều hiển thị lỗi ban
đầu, từ chối payload sai và giữ dữ liệu cũ khi refresh lỗi. Sau mỗi loại lỗi,
gửi lại kết quả thành công qua nút host làm dữ liệu xuất hiện và xóa lỗi.

| Viewer  | Dữ liệu vẫn hiển thị khi refresh lỗi                                    |
| ------- | ----------------------------------------------------------------------- |
| Invoice | INV-LOCAL-001, ITEM-LOCAL, tổng 30, hai dòng dịch vụ giá 0              |
| Stock   | ITEM-LOCAL, Local Warehouse, tồn 12, giữ chỗ 2, dự kiến 10, giá trị 180 |
| Chart   | Local sales chart, Alpha 30 và Beta 20 vẫn có cột                       |
| KPI     | Local revenue, 1,250.00, tăng 12%, sparkline                            |
| Funnel  | Lead 10, Opportunity 5, Won 2, chuyển đổi 20%                           |

Hai dòng dịch vụ null/omitted item_code đều hiển thị. Click từng tên không mở
panel Item và không gửi erpnext_item_get; trace có một auto-refresh Invoice GET
hợp lệ, không coi đó là lookup Item. Control click Local Item vẫn mở panel và
gửi erpnext_item_get cùng stock balance cho ITEM-LOCAL. Không bấm Submit hoặc
gọi dữ liệu production.

Artifact lưu trong `plans/evidence/017/`: 16 PNG nguyên bản và
`browser-trace.json` chứa 31 DOM snapshot thực cùng console log. SHA-256 JSON:
`01482c2de159ea8a5b6a132c0a021bee0312ab6976ebeb853c295593decd4765`. Parent kiểm
lại đầy đủ 15 snapshot lỗi, các snapshot recovery, service/control và số lượng
PNG bằng assertion trên artifact đã lưu.

Console: **0 error, 3 warning** từ Recharts về kích thước -1 lúc đo container.
Các ảnh và DOM sau đo vẫn có biểu đồ Alpha/Beta. Không tuyên bố console hoàn
toàn không cảnh báo hoặc đã chứng minh cảnh báo tồn tại từ baseline. Một lần
wait locator thất bại vì mới chọn scenario nhưng chưa bấm Đặt lại; đã đọc DOM,
bấm nút hiển thị và kiểm lại. Lần thất bại đó không tính là PASS.

Giới hạn: fixture local, không ERPNext thật, không mobile hoặc chứng cứ Node
20/22. Artifact Browser không thay thế CI với import map JSR gốc. Review và CI
trên đúng HEAD cuối vẫn bắt buộc trước merge.

## Tích hợp main và review artifact

Merge main `341cba437dba69348b6e11e2c6f599480d5fc212` sạch tại
`a0712504c7df6bc7097a21752e5499711ad61d23`; source UI 017 không đổi, chỉ tích
hợp regression và sửa locking Kanban 010 đã được duyệt. Parent tự chạy
server/browser check, lint 198 file, format 236 file và full suite **978 passed,
0 failed, 4 ignored**, diff check: đạt. Bundle UI/Node đã kiểm trước merge vẫn
cùng source vì main 010 không đổi UI hoặc runtime bundle code.

Reviewer độc lập APPROVE HEAD a0712504, kiểm đủ 16 PNG và 31 DOM snapshot cùng
hash JSON, kiểm source 017 không đổi và tự chạy lại 35 test trọng tâm. Đây là
review artifact Browser của parent, không phải lượt Browser độc lập thứ hai.
Không che ba warning Recharts hoặc coi chúng là baseline đã chứng minh.
