# Kế hoạch 005: Bằng chứng thực thi

Trạng thái: IN_PROGRESS. Review độc lập và gate local sau tích hợp main đã đạt;
chờ CI JSR thật và Codex review đúng HEAD trước merge.

## Mốc và kiểm tra phạm vi

- Worktree: nhánh `advisor/005-analytics-currency-context`, base
  `c1e74851077a1aff262c13116ce1d8f448302234`.
- `git status --short`: sạch trước khi sửa.
- `git diff --stat d2c5305..HEAD --` toàn bộ phạm vi: rỗng.
- `git diff --` toàn bộ phạm vi: rỗng.
- Không truy cập ERPNext production. Nguồn dưới đây là mã upstream công khai,
  không phải bằng chứng về custom field hoặc cấu hình của một site cụ thể.

## Ma trận đơn vị và kết quả local

Nguồn ERPNext nhánh version-15 đã ghim revision
`1a0bf0bf6c4aeaae5acde90c74b186312f49b95c`. Tất cả đường tiền tệ bên dưới chỉ
được tự chọn company khi người gọi nhìn thấy đúng một Company. Đồng tiền đích là
`Company.default_currency`, thiếu dữ liệu hoặc lỗi quyền phải truyền lỗi.

| Tool                        | Trường hiện tại và đơn vị upstream                                                                        | Company ownership và chính sách                                                                                                                                            | Trạng thái     |
| --------------------------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| `erpnext_stock_chart`       | `Bin.stock_value` là tiền công ty, nhưng không trả ra; `actual_qty` là số lượng                           | Loại phép cộng tiền chết; giữ chart số lượng và phạm vi cũ                                                                                                                 | Đạt unit local |
| `erpnext_sales_chart`       | Invoice `grand_total` theo currency chứng từ; Item `amount` theo currency cha                             | `Sales Invoice.company`; dùng `base_grand_total`, `base_amount`; child chỉ theo tập parent hợp lệ                                                                          | Đạt unit local |
| `erpnext_revenue_trend`     | Order `grand_total` theo currency chứng từ                                                                | `Sales Order.company`, dùng `base_grand_total` đã ghi nhận                                                                                                                 | Đạt unit local |
| `erpnext_order_breakdown`   | Order `grand_total` theo currency chứng từ                                                                | Như trên, giữ số lượng theo status                                                                                                                                         | Đạt unit local |
| `erpnext_revenue_vs_orders` | Order `grand_total` theo currency chứng từ                                                                | Như trên, trục count vẫn là số đơn                                                                                                                                         | Đạt unit local |
| `erpnext_stock_treemap`     | `Bin.stock_value` theo company của kho                                                                    | Tra `Warehouse.company`, không lọc `Bin.company` giả                                                                                                                       | Đạt unit local |
| `erpnext_product_radar`     | `Bin.stock_value`; Order Item `amount`; hai chiều còn lại là count/qty                                    | Kho và parent Order cùng company; dùng `base_amount`; chuẩn hóa 0-100 vẫn không có đơn vị tiền                                                                             | Đạt unit local |
| `erpnext_price_vs_qty`      | `Item Price.price_list_rate` theo `currency`; fallback `Bin.valuation_rate` theo company/stock UOM        | Item Price không có company. Không dùng tỷ giá hiện tại để đổi; dữ liệu giá khác currency đích phải báo lỗi rõ. Số lượng Order theo parent company; fallback qua Warehouse | Đạt unit local |
| `erpnext_kpi_revenue`       | Order `grand_total`; delta và sparkline dùng tiền                                                         | `Sales Order.company`, `base_grand_total` cho cả hai kỳ                                                                                                                    | Đạt unit local |
| `erpnext_kpi_outstanding`   | Invoice `outstanding_amount` là `party_account_currency`, không nhất thiết currency chứng từ hoặc company | Report Accounts Receivable đọc PLE `amount` trong company currency; chỉ lấy Sales Invoice, số dư dương, không cộng dòng tổng                                               | Đạt unit local |
| `erpnext_kpi_orders`        | Query `grand_total` nhưng chỉ dùng độ dài danh sách                                                       | Bỏ field tiền không dùng; giữ count scope hiện tại                                                                                                                         | Đạt unit local |
| `erpnext_kpi_gross_margin`  | Order Item `amount`; Bin `valuation_rate` theo stock UOM                                                  | `base_amount` cùng company với kho; phải dùng `stock_qty`, không `qty` nếu UOM khác. Giữ ước tính hiện tại, thiếu cost phải lỗi, không giả 0                               | Đạt unit local |
| `erpnext_kpi_overdue`       | Invoice `outstanding_amount` là party account currency                                                    | Report như outstanding, due date trước hôm nay, count invoice duy nhất                                                                                                     | Đạt unit local |
| `erpnext_sales_funnel`      | Opportunity `opportunity_amount`, Quotation/Order `grand_total` theo currency chứng từ                    | Company trên cả ba DocType; `base_opportunity_amount`/`base_grand_total`. Lead count giữ toàn phạm vi được phép đọc, mô tả rõ khác scope company                           | Đạt unit local |
| `erpnext_ar_aging`          | Invoice `outstanding_amount` là party account currency                                                    | Report như outstanding; giữ bucket chart, không đếm tổng hai lần                                                                                                           | Đạt unit local |
| `erpnext_gross_profit`      | Invoice Item `amount`; Bin `valuation_rate` theo stock UOM                                                | `base_amount`, parent Invoice và Warehouse cùng company; `stock_qty`; giữ chọn giá vốn cao nhất hiện tại và ghi rõ ước tính                                                | Đạt unit local |
| `erpnext_profit_loss`       | Cột Currency và report summary từ P&L chuẩn                                                               | Đã lọc company; tái sử dụng resolver không hard-code tên tool, giữ regression report                                                                                       | Đạt unit local |

## Nguồn xác minh chính thức

- [Sales Invoice schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/accounts/doctype/sales_invoice/sales_invoice.json):
  `grand_total.options=currency`,
  `base_grand_total.options=Company:company:default_currency`,
  `outstanding_amount.options=party_account_currency`.
- [Sales Order schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/selling/doctype/sales_order/sales_order.json)
  và
  [Quotation schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/selling/doctype/quotation/quotation.json):
  cùng quy tắc `grand_total`/`base_grand_total`.
- [Sales Invoice Item schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/accounts/doctype/sales_invoice_item/sales_invoice_item.json)
  và
  [Sales Order Item schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/selling/doctype/sales_order_item/sales_order_item.json):
  `base_amount`, `stock_qty`, `conversion_factor`, không có company field.
- [Opportunity schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/crm/doctype/opportunity/opportunity.json):
  `base_opportunity_amount` mang company currency.
- [Bin schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/stock/doctype/bin/bin.json),
  [Warehouse schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/stock/doctype/warehouse/warehouse.json)
  và
  [Stock Ledger Entry schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/stock/doctype/stock_ledger_entry/stock_ledger_entry.json):
  kho sở hữu company; stock value và valuation rate của ledger là company
  currency. Đã đối chiếu
  [đường cập nhật Bin](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/stock/stock_ledger.py#L1729-L1739):
  Bin nhận trực tiếp stock_value và valuation_rate từ SLE.
- [Item Price schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/stock/doctype/item_price/item_price.json):
  currency riêng, không có company hay tỷ giá lịch sử.
- [Company schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/setup/doctype/company/company.json):
  `default_currency` là Link Currency.
- [Accounts Receivable implementation](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/accounts/report/accounts_receivable/accounts_receivable.py):
  dòng 301-304 chọn PLE `amount` nếu không `in_party_currency` và không
  `party_account`; dòng 538-541 ghi `row.currency=company_currency`. Không có
  filter `presentation_currency` trong implementation hoặc file JS.
- [Payment Ledger Entry schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/accounts/doctype/payment_ledger_entry/payment_ledger_entry.json):
  `amount.options=Company:company:default_currency`;
  `amount_in_account_currency` theo `account_currency`.

## Policy đã được parent chốt trước triển khai

- `presentation_currency` trong kế hoạch không tồn tại ở report AR revision này.
  Parent duyệt khóa `in_party_currency: 0`, bỏ `party_account`, không tạo filter
  giả; kiểm currency từng row và chỉ lấy Sales Invoice, tắt chia payment terms
  và group subtotal để giữ count invoice.
- Giá vốn hiện tại vẫn chỉ là ước tính từ Bin, không phải giá vốn lịch sử. Sửa
  UOM từ qty sang stock_qty đã được parent duyệt trước sửa để nhân đúng đơn vị.
- Parent duyệt scatter chỉ chấp nhận giá selected cùng currency và stock UOM đã
  xác minh của Item; không đổi FX/UOM hiện tại, không bỏ giá lỗi rồi fallback.
- Mọi phép tổng còn cap vẫn ghi rõ giới hạn; không đưa pagination của 006 vào
  đây.

## Thực thi và gate

- Đã đọc trọn kế hoạch gốc và `docs/jsr-403-workaround.md`.
- Deno 2.9.5, Node v26.7.0; không nâng runtime hoặc dependency.
- Local dùng config import-map từ worktree003 và vendor
  `@casys/mcp-server@0.25.0`, `diff -qr` vendor với donor rỗng. `deno.lock`
  trước/sau cùng SHA-256
  `f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`. Các
  artifact này được Git bỏ qua; không đưa vào commit.
- Baseline `analytics_test.ts` + `query-report_test.ts`: 47 passed, 0 failed.
- Regression đỏ trên code cũ: 2 ca `analytics currency` đều fail đúng lỗi:
  expected VND nhưng actual EUR; multi-company không reject.
- Regression xanh: fixture USD 10 và EUR 20 có base VND 250000/560000 trả 810000
  VND; refresh giữ company và query dùng base field/company filter.
- Test context: company duy nhất, explicit VND/USD, nhiều/không company, lỗi
  quyền, currency thiếu, số tiền thiếu, child/Bin ownership và tập scope rỗng.
- Ma trận VND: mỗi money tool có expected số cụ thể. Radar giữ 0-100; scatter
  kiểm stock UOM, giá dạng số/chuỗi, giá mới nhất bằng 0, giá foreign không liên
  quan, giá selected foreign, thiếu UOM và permission; fallback VND được kiểm.
- Gross fixture dùng qty khác stock_qty, expected margin 60%; thiếu cost reject.
- AR fixture có hai account cho cùng invoice: cộng số dư riêng nhưng count chỉ
  một voucher. Lặp chính khóa `party_account/voucher_type/voucher_no/party`
  reject. Subtotal và Journal Entry không được cộng. Không đổi
  FINANCIAL_REPORTS.
- P&L giữ toàn bộ regression report và thêm company vào refresh đã có.
- Schema validator kiểm input cũ `{}` và input có company cho 14 tool được
  migrate; mỗi tool cũng phải reject company ambiguity và lỗi quyền.

### Lệnh và kết quả cuối

Tất cả lệnh sau chạy trong worktree005, trừ lệnh npm/UI được ghi thư mục rõ:

```bash
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/
# 889 passed | 0 failed | 4 ignored

deno check --config deno.nojsr.json --sloppy-imports --frozen mod.ts server.ts
# exit 0

deno lint --config deno.nojsr.json
# exit 0, 193 files

deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/
# exit 0, 216 files

git diff --check
# exit 0
```

UI: trong `src/ui`, `npm ci --offline --no-audit --no-fund` cài đúng lock 208
packages; `node build-all.mjs` exit 0, đủ 7 viewer. Node:
`npm_config_offline=true bash scripts/build-node.sh` exit 0, framework 0.25.0;
`node --check dist-node/bin/hvgerp-mcp.mjs` exit 0. Trong `dist-node/bin`,
`npm pack --dry-run --json` exit 0: 10 file gồm bundle, README, package.json và
đúng 7 HTML viewer; không publish.

### Mở scope tối thiểu đã được duyệt

Full suite đầu tiên sau migration: `885 passed | 1 failed | 4 ignored`. Lỗi duy
nhất là fixture auto-select của `src/client_test.ts:238` trả Company rỗng.
Parent tự đọc test và duyệt thêm đúng fixture vào scope: Company list/get trả
company/currency, giữ hai assertion quá giới hạn không có query và auto-select
có query. Counter GET cũng được tính để bảo vệ trước mọi round-trip. Không sửa
source client, không bỏ hoặc nới test.

## Giới hạn bàn giao

- Chưa CI JSR thật, chưa browser QA với payload này, chưa review độc lập hoặc
  PR; parent quản lý các gate này. Local fixtures không phải ERPNext thật.
- Chưa chạy Node 20/22 hoặc Docker smoke; đây không phải gate đã hoàn tất.
- Không có `deno task release:check` local thành công: workaround chỉ dùng
  underlying commands, không nói nó là CI JSR hoặc release preflight đầy đủ.
- Parent/Warehouse lookup mới có cap 1000 và child/Bin/document query giữ cap
  cũ; tổng vẫn có thể thiếu dữ liệu. Kế hoạch006 phải xử lý cả cap scope mới.
- Gross margin không thành actual GL gross profit. Giá bán không chuyển FX/UOM
  bằng tỷ giá hoặc conversion hiện tại. Lead vẫn đếm theo quyền nhìn thấy, không
  bịa field company.
- Đã tự đọc diff source, test và docs. Không sửa version, dependency,
  production, root userfiles hoặc `plans/README.md`.

## Tích hợp main sau review

- Đã merge thông thường `origin/main` tại
  `0cf6a69463fef96f95512d36dda92ec2ad286f22` vào commit implementation
  `d2556d905565d033b35d8d964c0c178ddb0d9c82`, không conflict. Merge commit:
  `0351d0bd40c7af5a648d69fda55f5f39b387bc5c`.
- Source, test và docs trong scope 005 không đổi qua merge. Thay đổi nhận từ
  main thuộc kế hoạch 007 đã được parent duyệt và merge.
- Đồng bộ `publish.exclude` trong cấu hình ignored `deno.nojsr.json` với
  `deno.json`: thêm `src/ui/testing/**`. Mọi trường ngoài `imports` khớp cấu
  hình chính; `imports` vẫn khớp donor 003. Vendor pristine và lock giữ nguyên
  SHA-256 `f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`.
- Chưa chạy lại gate tích hợp sau merge, chưa push. Parent sẽ chạy các gate trên
  commit bàn giao; kết quả local phía trên chỉ áp dụng trước merge.

## Review độc lập và gate tích hợp của parent

- Reviewer độc lập APPROVE source `d2556d905565d033b35d8d964c0c178ddb0d9c82`: tự
  kiểm regression đỏ trên base, đối chiếu khóa ledger với upstream, chạy 104
  test focused và toàn suite 889 passed, 0 failed, 4 ignored; typecheck, lint,
  format, UI và Node đạt.
- Parent đọc toàn bộ delta source/test/docs, kiểm lại chính sách company,
  currency, stock UOM và report AR bằng source upstream đã ghim ở trên.
- Sau tích hợp main `e09537b25e133c21b2c1915b15937d78c6dd0bbc`, HEAD
  `7cf7c99f7853f0b1a90209bd01bb30c812b4328e` không đổi source trong scope 005.
  Parent chạy lại: browser TypeScript, server typecheck, lint 197 file, format
  229 file, toàn suite **901 passed, 0 failed, 4 ignored**.
- Build đủ 7 UI, Node bundle và `node --check` đạt. Pack dry-run từ
  `dist-node/bin` có đúng 10 file, gồm 7 HTML, không chứa test host; không
  publish.
- Các lệnh Deno local tiếp tục dùng config workaround và `--frozen` như phần
  trên. Kết quả này không thay thế CI dùng JSR thật, không xác nhận Node 20/22,
  Docker hoặc ERPNext production. Không có bằng chứng browser render payload
  tiền tệ của 005 và không nhận QA của 007/008 làm bằng chứng thay thế.

## Sửa finding PR 29, vòng review ngày 2026-09-05

Baseline sửa: `b896576a1a8023b7030fe52738ec12c415a13a4c`, worktree sạch trước
delta. Executor đọc đầy đủ kế hoạch 005, skill improve execute, web-access và
workaround JSR. Hai finding được đọc nguyên văn qua GitHub API:

- [3939702181: URL ownership quá dài](https://github.com/hvgllc/hvgerp-mcp/pull/29#discussion_r3939702181).
- [3939702186: snapshot AR dùng ngày UTC](https://github.com/hvgllc/hvgerp-mcp/pull/29#discussion_r3939702186).

### Đối chiếu source và chính sách chunk đã duyệt

`FrappeClient.list` serialize fields, filters, order_by, limit, limit_start và
as_dict qua URLSearchParams vào GET `/api/resource/...`. Không sửa client này.
Parent và executor cùng đọc Frappe tại commit
`755b5cb81fabb431265690fca07f4a8038a5599a`:

- [api/v1.py](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/api/v1.py#L10):
  `document_list` chuyển đến `frappe.client.get_list`, mặc định limit 20.
- [client.py](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/client.py#L28):
  `order_by=None`, đi qua `validate_args` trước `frappe.get_list`.
- [reportview.py](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/desk/reportview.py#L98):
  `validate_args`, `parse_json` và `setup_group_by` không thêm thứ tự mặc định
  cho các truy vấn không aggregate này.
- [db_query.py](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/model/db_query.py#L1495):
  `set_order_by` không bảo đảm dùng modified khi đầu vào None. Vì vậy không gọi
  modified là default đã được ERP bảo đảm.

Parent duyệt lấy tối đa N ứng viên mỗi chunk, merge theo thứ tự rồi cắt N toàn
cục. Các caller đã có `base_amount desc` hoặc `stock_value desc` giữ thứ tự đó.
Caller trước đây không khai báo order nay dùng `modified desc` tường minh, thêm
field phục vụ merge. Gross KPI vẫn chọn giá Bin dương đầu tiên trong tập đã sắp;
đây là ước tính, không phải actual GL. Tie giữ thứ tự chunk rồi thứ tự server
trong chunk, không tuyên bố tương đương collation/tie ordering của database.

Helper dùng budget 6.000 byte cho API path và query đã encode, bao gồm mọi
field/filter/order/limit liên quan, không chỉ đếm ID. Budget chừa headroom,
không bảo đảm cho proxy có prefix tùy ý dài hoặc giới hạn nhỏ hơn. Tên đơn lẻ
kèm options không vừa phải lỗi rõ, không bỏ ID. Ownership kiểm theo chính chunk
được hỏi, không chỉ theo tập company tổng. Không đổi cap 1.000 parent/kho hoặc
cap N đầu ra; lượng ứng viên đọc có thể N nhân số chunk. Đây không phải giải
pháp pagination/completeness của kế hoạch 006.

### Red/green và gate local cho delta

- Sáu test site-date mới đỏ đúng ngày lệch một ngày, `0 passed, 6 failed`, trước
  sửa. Sau sửa, outstanding/overdue/aging đều gọi một `siteToday(ctx)` và dùng
  ngày đó cho report cùng phân loại. Test Asia/Ho_Chi_Minh và
  America/Los_Angeles, các mốc -1/0/1/30/31/60/61/90/91 ngày; mô phỏng report
  kết thúc sau hai ngày vẫn giữ snapshot ban đầu. Thêm ba test fallback timezone
  và lỗi quyền report được truyền nguyên vẹn, không sửa policy của
  `site-date.ts`.
- Sáu test URL mới đỏ HTTP 414 trước sửa, `0 passed, 6 failed`. Mock fetch dùng
  URL thật do FrappeClient tạo và từ chối trên 8.192 ký tự; không thay list bằng
  mock bỏ qua serialization. Sau sửa, cả Sales Order, Sales Invoice và Warehouse
  qua được với 1.000 tên hoặc tên Unicode dài; top N ở chunk cuối vẫn được lấy,
  mọi scope ID đều được hỏi. Có regression cross-chunk ownership, permission,
  thiếu modified, tên đơn lẻ quá lớn và tie/global cap. Đây là fixture, không
  phải ERPNext hoặc proxy thật.
- Test trọng tâm với
  `--config deno.nojsr.json --sloppy-imports --frozen
  --allow-all`:
  `analytics-context_test.ts`, `analytics_test.ts`, `query-report_test.ts` đạt
  **108 passed, 0 failed**.
- `deno check --config deno.nojsr.json --sloppy-imports --frozen mod.ts server.ts`
  exit 0. `deno lint --config deno.nojsr.json`: exit 0, 197 file.
- UI `npm ci --offline --no-audit --no-fund`, `npm run typecheck`,
  `node build-all.mjs`: exit 0, đủ 7 viewer. Full suite chạy sau UI build:
  `deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/`
  đạt **920 passed, 0 failed, 4 ignored**.
- `npm_config_offline=true bash scripts/build-node.sh` và
  `node --check dist-node/bin/hvgerp-mcp.mjs`: exit 0.
  `npm pack --dry-run --json` tại `dist-node/bin`: exit 0, 10 file với đủ 7
  HTML, không publish.
- Deno 2.9.5, Node 26.7.0. Lock SHA-256 vẫn
  `f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`, vendor
  diff với donor 003 rỗng; config ngoài imports khớp deno.json, imports khớp
  donor.

- `deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/`: exit 0,
  229 file. `git diff --check`: exit 0. Đã tự đọc toàn bộ diff source/test/docs,
  chỉ thay đổi 8 file trong scope 005.

Delta cần review mới và CI JSR thật sau push do parent quản lý; chưa tự push,
merge main mới, đổi dependency/version hoặc thao tác production. Browser
precheck web-access timeout; GitHub API và raw upstream đọc được. Không có QA
browser/ERPNext thật cho delta này và không dùng gate trước delta để thay thế.

### Gate tích hợp sau sửa finding và main PR 30

Delta đã commit local tại `d4cd7f25014e9c2899fd7f8d34d369c2966d2ad6`. Theo chỉ
thị mới của parent, merge main `99b1fa319590e60730faabdb033a5b48a44e1862` trên
worktree sạch, không conflict, tạo `407b071a039c031a987f0e709b52e1b35be3fbcd`.
Diff toàn bộ source/test/docs và evidence 005 qua merge rỗng; chỉ nhận source và
evidence 009 đã được merge.

Gate chạy lại trên HEAD tích hợp:

- Server check exit 0; lint 200 file và format 235 file exit 0, cùng flags
  workaround ở trên. `git diff --check` exit 0.
- UI `npm run typecheck` rồi `node build-all.mjs`: exit 0, đủ 7 viewer.
- Full suite chạy sau UI build: **960 passed, 0 failed, 4 ignored**. Lệnh
  `deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/`.
- Node build offline và `node --check` exit 0; pack dry-run từ `dist-node/bin`
  exit 0, đúng 10 file gồm 7 HTML. Không publish.
- Lock SHA-256, vendor pristine và cấu hình workaround kiểm lại không đổi.
  Worktree tracked sạch trước append evidence này.

Chưa push. Parent quản lý review mới trên delta, CI JSR thật và các finding PR
còn mở; không coi approval hoặc CI của source cũ là gate cho delta mới.

### Review độc lập và kiểm tra của parent sau hai finding

Reviewer độc lập chấp thuận HEAD `9e0853ec758e3aef562a5878840478c7b2ee28fe`, sau
khi đọc nguyên văn hai finding, toàn bộ delta source/test/docs và tự chạy 108
test trọng tâm, server typecheck, `git diff --check`. Không còn finding trong
delta đã review.

Parent đọc toàn bộ implementation, regression và tài liệu; tự chạy lại server
check, lint 200 file, format 235 file, full suite **960 passed, 0 failed, 4
ignored**, Node syntax check và diff check, đều exit 0. Diff source/test/docs
005 từ commit sửa `d4cd7f25014e9c2899fd7f8d34d369c2966d2ad6` đến HEAD được duyệt
rỗng. Phần thêm này chỉ lưu bằng chứng, không thay source. CI JSR thật và Codex
review phải chạy trên HEAD mới sau commit bằng chứng trước khi merge.

## Revision ownership discovery và Item UOM, review 5120318164

Đã đọc nguyên văn hai finding do parent cung cấp trên
`0eced8c8c7bcb9e34f90ee953296e2ba9afb0ef9`:

- [3939863466: phân trang ownership trước khi lọc child](https://github.com/hvgllc/hvgerp-mcp/pull/29#discussion_r3939863466).
- [3939863468: chia lookup Item stock UOM](https://github.com/hvgllc/hvgerp-mcp/pull/29#discussion_r3939863468).

Cả hai được sửa. Các ghi chú cap ownership 1.000 ở phần lịch sử phía trên không
còn mô tả implementation mới. Cap child/Bin/document và giới hạn phép tổng cũ
vẫn còn, thuộc 006; revision này không tuyên bố hoàn tất 006.

### Source và tích hợp

Worktree sạch tại đầu revision. Đã đọc plan 005 và evidence đầy đủ, đối chiếu
AGENTS với bản đã đọc, không có delta; đọc lại workaround JSR ở root. Merge main
`341cba437dba69348b6e11e2c6f599480d5fc212` sạch thành
`53f54bc221fd33d96618aac9dc91faa8c0fe2970`, không conflict, chỉ nhận regression
010 đã được parent duyệt. Source/tests/docs của revision:
`10bd20f3395ab5a29d57d39c419a1e7ae186c9c2`.

- Ownership discovery dùng trang 1.000 tên, `name asc`, `limit_start`, giữ
  company và docstatus filters. Chỉ đọc child/Bin sau khi discovery hoàn tất.
  Trang đủ 1.000 cần đọc tiếp, kể cả trang rỗng cuối khi tổng là bội số 1.000.
- Tên thiếu, sai kiểu, trống, trùng trong/cross-page, trang vượt kích thước yêu
  cầu hoặc lỗi trang sau đều throw. Giới hạn an toàn 100.000 tên mỗi lookup:
  vượt thì lỗi rõ, không dùng tập thiếu làm scope. Warehouse vẫn memo promise
  trong đúng context, kể cả kết quả lỗi; không thêm registry hoặc public config.
- Tách nguyên logic chia request từ listScoped để Item UOM dùng chung budget
  6.000 byte path/query đã encode. Tính cả fields, filters, order, limit theo
  serialization của FrappeClient. Tất cả chunk được kiểm trước lần gọi Item đầu
  tiên; một ID quá dài không bị bỏ qua.
- Item lookup yêu cầu đúng đủ ID của từng chunk, không nhận duplicate hoặc ID
  của chunk khác. Input ID trùng được gộp; thứ tự response không ảnh hưởng điểm
  chart. Lỗi chunk sau được truyền nguyên vẹn, không trả chart một phần hoặc
  fallback Bin. Currency, stock UOM, giá mới nhất và refresh company vẫn được
  kiểm theo hợp đồng cũ.
- Đã audit các `name in`, `parent in`, `warehouse in` trong analytics/context:
  lookup mới của 005 đều qua helper bounded. Account name-in ở P&L có từ trước
  delta 005, không sửa ngoài revision. Không thay query-report, framework,
  dependency, version, schema hoặc response shape.
- Cập nhật cả docs/tools.md và docs/concepts.md vì cả hai đang tuyên bố cap
  ownership 1.000. Giữ quy ước tiếng Anh của tài liệu hiện có.

### Red/green có kiểm tra hành vi

Baseline sau merge: 108 passed, 0 failed với analytics/context/query-report.
Lượt test discovery đầu bị lỗi kiểu tuple trong fixture mới, chưa phải red hành
vi. Đã sửa tuple, trả source về HEAD bằng apply_patch rồi chạy red thực: **24
passed, 24 failed** trong context. Các ca thất bại là không đọc trang tiếp,
không truyền lỗi/tên lỗi trang sau hoặc không phát hiện trang lặp. Sau áp dụng
source discovery: **135 passed, 0 failed** cho ba file trọng tâm.

Red UOM trên source lookup Item một request: **72 passed, 7 failed** trong
analytics. Ba loại URL vượt budget, ID đơn lẻ quá dài, chunk sau không được gọi,
duplicate và out-of-scope mapping đều tái hiện. Control missing Item vốn đã lỗi
đúng tiếp tục đạt. Green đầu còn một fixture cũ trả ITEM-2 khi query chỉ hỏi
ITEM-1: đã sửa mock để tôn trọng name-in, giữ nguyên mọi assertion nghiệp vụ.

Green cuối: **146 passed, 0 failed**, thêm 38 test so với baseline. Bao phủ:

- Cả Sales Order, Sales Invoice, Warehouse với 0/1.000/1.001/2.000 tên; top 2
  giá trị cao nằm ở trang cuối; exact-page có trang rỗng cuối; giữ filter
  company và docstatus; không child query khi scope rỗng hoặc discovery lỗi.
- Cả ba ownership source có page sau lỗi, repeat, thiếu tên, tên trắng hoặc sai
  kiểu. Thêm duplicate trong cùng trang, oversized page, guard 100.000 và memo
  Warehouse không chạy lại khi promise đã lỗi.
- Fixture URL thật dùng FrappeClient cho ownership 1.001 tên, giữ global top N
  và mọi ID được hỏi; phiên bản tên Unicode dài giữ regression chunk cũ.
- Scatter 200 Item codes thường, dài và Unicode: mọi Item path/query thật không
  vượt 6.000, mỗi nhóm có nhiều request, union chunk bằng đúng input. Response
  Item đảo thứ tự vẫn tạo đúng toàn bộ 200 điểm, số tiền VND và stock quantity.
- Oversized ID lỗi trước mọi Item request, lỗi chunk thứ hai không bị nuốt;
  duplicate, missing, ID thuộc chunk khác đều bị từ chối; tập rỗng không query
  và input ID trùng không tạo duplicate lookup.

### Gate cuối và hiện vật

```sh
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/tools/analytics-context_test.ts src/tools/analytics_test.ts src/tools/query-report_test.ts
deno check --config deno.nojsr.json --sloppy-imports --frozen mod.ts server.ts
deno lint --config deno.nojsr.json
deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/
npm ci --offline --no-audit --no-fund --prefix src/ui
npm --prefix src/ui run typecheck
node src/ui/build-all.mjs
npm_config_offline=true bash scripts/build-node.sh
node --check dist-node/bin/hvgerp-mcp.mjs
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/
git diff --check
cd dist-node/bin
npm pack --dry-run --json --offline
```

Tất cả gate cuối exit 0. Full suite **1.046 passed, 0 failed, 4 ignored**; check
server đạt, lint 200 file, format 236 file. UI typecheck và đủ 7 viewer đạt;
Node bundle 1,8 MB và syntax check đạt. Pack dry-run đúng thư mục publishable có
10 file, gồm 7 HTML, bundle, package.json và README; không test assets, không
publish. Không chạy full suite song song UI build. Build sau source fix; sau
build chỉ bổ sung test ownership 1.001 tên, docs và evidence, không đổi runtime
source. Đã đọc đầy đủ diff trước commit source.

Config ngoài imports bằng deno.json, đã kiểm bằng Node assert; vendor diff với
npm 0.25.0 nguyên bản trong Deno cache rỗng. Lock frozen SHA-256 giữ nguyên
`f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`. Local Deno
2.9.5 và Node 26.7.0, không nâng dependency. Các log tạm trong thư mục cha
worktree: `005-revision-baseline.log`, `005-discovery-red.log`,
`005-discovery-green.log`, `005-uom-red.log`, `005-uom-green.log`,
`005-revision-full.log`, `005-revision-ui-install.log`,
`005-revision-ui-build.log`, `005-revision-node-build.log`.

Giới hạn: không có transaction snapshot xuyên các page; dữ liệu thay đổi đồng
thời có thể làm lookup lỗi khi lặp tên, không tuyên bố snapshot kế toán nguyên
tử. Guard ownership là fail-fast, không cap ngầm. Budget request không bảo đảm
proxy có prefix tùy ý hoặc giới hạn nhỏ hơn 6.000. Không ERPNext production,
browser QA, Node 20/22 hoặc CI JSR thật trong vòng local này. Parent quản lý
fresh review, CI, push, reply và merge; executor chỉ local commit, chưa tự
duyệt.
