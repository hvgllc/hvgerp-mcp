# Bằng chứng thực thi thiết kế 023

## Phạm vi và mốc nguồn

- Base: `174cd29bd5bd7ced3cb231b56e786e4f982e422c`.
- Nhánh: `advisor/023-stock-ledger-timeline-design`.
- Commit tài liệu thiết kế: `48bfdcca731044ce9839b07612ed4bdad43b6f3c`.
- Chỉ tạo `docs/design/stock-ledger-timeline.md` và evidence này. Không sửa
  source, schema/API, dependency, roadmap, plan/index hoặc manifest.
- Executor dùng `improve execute` và closing-the-loop; parent giữ review độc lập
  và trạng thái index. Đây là hoàn tất tài liệu local, không tự APPROVE và không
  chứng minh timeline đã triển khai.

Đã đọc toàn bộ kế hoạch 023 được parent cấp ở root, AGENTS, skill/closing,
`docs/ROADMAP.md`, thiết kế Customer360 đã merge và source liên quan. Root index
ghi 015 DONE, 023 IN_PROGRESS. Quote Stock Ledger Timeline ở roadmap dòng 38
khớp, dependency 015 có tool mới đúng contract. Trước sửa, ba lệnh status/diff
của kế hoạch đều không có thay đổi; không có drift chưa giải thích.

Nguồn chính đã đối chiếu: inventory ledger handler/schema, generic list/get
operations, list-result, stock chart analytics, ChartData và click handler,
doclist local pagination/row action/inline mutation buttons, movement parser,
resolver và caller client. Chứng cứ schema upstream ghim revision của 015 được
đọc từ `plans/evidence/015.md`; 023 không truy cập upstream mới hoặc ERP site.
Một lần thử đường dẫn `chart-viewer/src/types.ts` trả lỗi không tồn tại; đã đọc
import thực và `src/ui/shared/presentation.ts`, không tạo file để khớp giả định.

## Kết quả từng bước

1. Lập bảng observed/unverified, đủ bảy heading rubric và hai phương án A/B. Đã
   ghi đúng giới hạn 015: limit 1..20/default 5, item ID hoặc tên, warehouse ID,
   fresh ledger list, không company/date/offset/count/viewer binding. Không gán
   API pagination hoặc transform chart tưởng tượng cho composition.
2. Viết C01-C10: identity/company, ngày/timezone, chronology, delta/balance,
   UOM/currency, pagination/budget, opening, cancellation/freshness, quyền/lỗi,
   point drill-down. Query Q là cấu hình generic dự kiến, company còn
   unverified; O thay cả hai bound ngày của Q bằng trước date_from, không thêm
   filter mâu thuẫn. Không suy opening 0 hoặc từ trang thiếu dữ liệu.
3. Viết 14 design walkthrough, mỗi ca có input, query/thao tác và expected
   output/error. Một vòng phản biện A chỉ ra thiếu offset, freshness option,
   chart transform/binding, exact point mapping và chronology balance. Quyết
   định defer phát hành timeline; A chỉ là hướng khảo sát thủ công sau approval
   riêng, B chưa được triển khai. Không mở rộng sang replenishment.

Đã tự đọc toàn tài liệu sau format và sửa câu có thể hiểu nhầm “không chạy Q
hoặc bỏ filter” thành cấm bỏ filter rõ ràng; làm rõ O thay bound ngày và điều
kiện completeness trước nối balance line. Không có reviewer độc lập trong lượt
executor; gate reviewer của kế hoạch vẫn do parent thực hiện.

## Lệnh và kết quả

| Kiểm tra                                                                          | Kết quả thực                                                                                     |
| --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `git status --short` trước sửa                                                    | Rỗng                                                                                             |
| `git diff --stat d2c5305..HEAD -- docs/design/stock-ledger-timeline.md` trước sửa | Rỗng                                                                                             |
| `git diff -- docs/design/stock-ledger-timeline.md` trước sửa                      | Rỗng                                                                                             |
| `rg -n '^## ' docs/design/stock-ledger-timeline.md`                               | Đủ đúng 7 heading bắt buộc                                                                       |
| `rg` kiểm ba heading Verification/Decision/Open questions                         | Có đủ ba phần cuối                                                                               |
| Node assertion rubric                                                             | 7 heading đúng thứ tự, 10 contract mục, 14 ca có đủ 4 cột, đủ ba nhãn, không U+2014              |
| Node assertion link so với `git ls-tree -r --name-only HEAD`                      | 15 link nội bộ đúng case; control `docs/roadmap.md` bị từ chối, `docs/ROADMAP.md` được chấp nhận |
| `deno fmt --check docs/design/stock-ledger-timeline.md plans/evidence/023.md`     | Exit 0                                                                                           |
| `git diff --check` và staged diff check                                           | Exit 0                                                                                           |
| Diff source/roadmap/dependency/index so với base                                  | Rỗng                                                                                             |

Kiểm link dùng tập path từ Git tree, không dùng existsSync trên macOS làm bằng
chứng đúng case. Mã kiểm rubric/link chạy transient bằng
`node --input-type=module
-e`, không thêm test file. Thao tác cốt lõi: lấy
heading `^## (.+)$`, đếm `^### C\d{2}:` và `^\| V\d{2}`, kiểm đủ bốn cột,
resolve link bằng `path.posix.normalize` từ thư mục tài liệu rồi so membership
chính xác với Git tree. Có negative control sai case, không sửa metadata Git.

## Giới hạn và bàn giao

- Không chạy Deno test/server check/lint, UI typecheck/build, Node bundle,
  Browser/Vite, production query hoặc external network: không có code change và
  kế hoạch này chỉ yêu cầu gate tài liệu. Không claim các gate runtime đạt.
- Bảng walkthrough là suy luận thiết kế trên dữ liệu giả, không phải test đã
  chạy. Unknown company/permission/timezone/chronology/opening được giữ thành
  câu hỏi chặn có cách xác minh; không dựng API để vượt điều kiện dừng.
- Không coi role upstream của 015 hoặc Browser/CI 015 là kiểm chứng site đích
  cho timeline. Không claim `sendMessage` là router typed hoặc viewer read-only
  đã được cưỡng chế.
- Cần reviewer chỉ đọc kiểm query/giả thuyết/14 ca trước khi parent đóng 023.
  Defer tính năng là quyết định hợp lệ của thiết kế, không phải thiếu tài liệu.
- Không push, PR, merge, release hoặc đổi roadmap thành shipped.

## Review độc lập do parent xác nhận

Reviewer độc lập đã APPROVE HEAD `3b073f53246ec0385bacfec7149e3f8890624a16`,
tree `f101e89405b8f50b7438bf36fbeb2020112809f1`, report blob
`f179dcb7a339b3fefe7c370a62f624fa5d88eacf`. Reviewer đọc đầy đủ plan, design,
evidence và đối chiếu source; kiểm đủ 7 mục, 2 phương án, 10 contract, 14
walkthrough, 15 link đúng case, format và diff check. Parent cũng đã đọc toàn bộ
design và evidence. Approval chỉ dành cho thiết kế, không chứng minh timeline đã
triển khai. CI và Codex trên PR vẫn là gate tiếp theo.
