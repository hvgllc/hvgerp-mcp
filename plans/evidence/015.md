# Chứng cứ thực thi kế hoạch 015

## Phạm vi và mốc nguồn

- Base: `341cba437dba69348b6e11e2c6f599480d5fc212`.
- Core commit: `0afef87a570201817e09a4599ec48b2986101d83`.
- Branch: `advisor/015-stock-item-movement-query`.
- Mục tiêu: Recent Movements chỉ đọc Stock Ledger Entry của mặt hàng và kho đang
  chọn, không gọi Stock Entry toàn site hay generic operations.
- Kiểm drift từ `d2c5305` đến base: chỉ panel có guard kiểu chuỗi từ 007;
  host/fixtures được tạo bởi 007 và mở rộng detail race bởi 009. Giữ nguyên các
  guard và scenario cũ. Worktree sạch trước khi sửa.

Executor đã tự đọc schema ERPNext v15 qua GitHub Contents API tại commit
`1a0bf0bf6c4aeaae5acde90c74b186312f49b95c`:
[Stock Ledger Entry schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/stock/doctype/stock_ledger_entry/stock_ledger_entry.json).
Nguồn có `item_code`, `warehouse`, `posting_date`, `posting_time`,
`voucher_type`, `voucher_no`, `actual_qty`, `qty_after_transaction`,
`stock_uom`, `is_cancelled` (Check, mặc định 0), cùng `name` chuẩn của DocType.
Schema có quyền đọc cho Stock User và Accounts Manager. Đây không phải bằng
chứng quyền đọc trên site triển khai cụ thể.

## Hợp đồng đã triển khai

- Tool mới `erpnext_stock_ledger_list` thuộc `inventory`, read-only, không có
  viewer binding riêng. Required: `item_code` và `warehouse`, chuỗi không rỗng;
  `limit` là integer 1 đến 20, mặc định 5. Kiểm tra trước cả resolve và query.
- Resolve Item ID hoặc tên bằng `resolveItem`; warehouse giữ ID chính xác. Query
  cố định 10 trường trong kế hoạch, ba filter `item_code`, `warehouse`,
  `is_cancelled = 0`; sắp xếp ngày, giờ, tên giảm dần. Kết quả `{data: rows}`.
  Handler không nhận DocType/fields/filter/order tùy ý; direct calls có trường
  thừa cũng không thể ghi đè query. MCP schema từ chối trường thừa.
- Panel dùng builder chung với limit 5, parse và kiểm kiểu từng dòng, từ chối
  dòng khác item hoặc warehouse. Hiện voucher, ngày/giờ, lượng thay đổi và UOM.
  Empty ledger có thông báo riêng; lỗi quyền đọc hiện rõ, không fallback.
- Effect giữ hai request độc lập chạy song song, phụ thuộc cả item và warehouse,
  xóa state khi đổi cặp, có cancellation guard và chặn render state thuộc cặp
  cũ. Không thêm package hoặc thay đổi public API của Stock Entry.

## Test và gate core

Môi trường local: Deno 2.9.5, Node 26.7.0. Dùng workaround đã duyệt từ
`docs/jsr-403-workaround.md`: donor 017, vendor npm `@casys/mcp-server@0.25.0`
khớp nguyên bản cache npm qua `diff -qr`; JSON config giống `deno.json` sau khi
bỏ riêng `imports`. Lock giữ SHA-256
`f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`.

Các lệnh Deno test/check dưới đây đều thêm
`--config deno.nojsr.json --sloppy-imports --frozen`.

| Kiểm tra                                                                                                             | Kết quả thực tế                                   |
| -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| Baseline `deno test --allow-all src/tools/inventory_test.ts src/client_test.ts`                                      | 25 đạt, 0 lỗi                                     |
| Red: thêm test tool/validation/permission/inventory-only trước implementation                                        | 25 đạt, 4 lỗi đúng hành vi tool chưa được đăng ký |
| Focused `deno test --allow-all src/ui/shared/stock-movements_test.ts src/tools/inventory_test.ts src/client_test.ts` | 34 đạt, 0 lỗi                                     |
| `deno check mod.ts server.ts`                                                                                        | exit 0                                            |
| `deno lint --config deno.nojsr.json`                                                                                 | exit 0, 200 file                                  |
| `deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/`                                                  | exit 0, 235 file trước evidence này               |
| `npm ci --offline --no-audit --no-fund --prefix src/ui`                                                              | exit 0, cài 208 package theo lock                 |
| `npm --prefix src/ui run typecheck`                                                                                  | exit 0                                            |
| `node src/ui/build-all.mjs`                                                                                          | exit 0, đủ 7 viewer                               |
| `npm_config_offline=true bash scripts/build-node.sh`                                                                 | exit 0                                            |
| `node --check dist-node/bin/hvgerp-mcp.mjs`                                                                          | exit 0                                            |
| `npm_config_offline=true npm pack --dry-run --json` tại `dist-node/bin`                                              | exit 0, 10 file, đủ 7 HTML, không có testing host |
| `deno test --allow-all src/` sau build                                                                               | 956 đạt, 0 lỗi, 4 ignored                         |
| `git diff --check`                                                                                                   | exit 0                                            |

Test mới bao gồm input thiếu/rỗng/sai kiểu, limit null/0/âm/21/phân số/chuỗi/
NaN/Infinity, Item resolution từ tên, lỗi resolution và ledger permission không
fallback, ba cặp item/kho với giới hạn 1/5/20, query overrides không có hiệu
lực, inventory-only client thật và handler map không chứa operations, parser
text/ structured/empty/malformed/cross-item/cross-warehouse.

Trong lúc viết test, một lần typecheck thất bại vì mock `FrappeAPIError` thiếu
tham số body; sửa mock trước khi ghi nhận red hành vi. Một lần happy-path mock
trả GET thành công nhưng kỳ vọng resolve sang ID khác; sửa fixture thành GET 404
rồi exact-name list theo hợp đồng `resolveItem`. Không đổi resolver để làm test
đạt. Không tính hai lần này là regression của code cũ.

Artifact core local, không commit generated output:

- Stock HTML SHA-256:
  `cd0d677911a66fd2d89e53c7b18c342fe555ad7111677c142b7a8c7e20ede7a4`.
- Node bundle SHA-256:
  `9ae3db57b191e00fdc6f84b4f09f4843b84cf8703c110760ef2db1b8d16d451d`.

## Phần còn chờ

### Đồng bộ catalog được parent phê duyệt

Parent đã đối chiếu và cho phép bổ sung đúng năm tool cũ bị thiếu vào hai bảng
`docs/tools.md` và `docs/coverage.md`: `erpnext_gl_entry_list`,
`erpnext_financial_report`, `erpnext_employee_checkin_list`,
`erpnext_attendance_day_get`, `erpnext_attendance_day_fix`. Đây là đồng bộ tài
liệu trong scope, không sửa source Accounting/HR. Heading Accounting 8 và HR 15
được đối chiếu registry thực. Viewer binding căn theo `_meta` từng declaration,
không suy từ category. `attendance_day_fix` ghi rõ mutation, có thể hủy và dựng
lại Attendance đã submit, cần `confirm_cancel_attendance`, lý do và ứng dụng
`hvg_workspace`; không gắn read-only.

- Parent đang điều phối bản sửa 017 chung host/fixtures. Executor 015 chưa sửa
  hai file đó tại mốc core này và không mở/dừng/restart host hoặc Browser.
- Chưa chạy Browser thật cho 015, chưa có screenshot hoặc trace. Build và unit
  test không thay thế Browser gate. Parent sẽ kiểm các cặp item/kho, lỗi quyền,
  empty/malformed, late response và trace inventory-only bằng fixture local.
- Registry thực là 135 tool / 16 category, inventory 10. Bốn catalog đã thêm
  ledger và số đếm; hai bảng cũ được bổ sung năm dòng thiếu như phần đồng bộ
  catalog đã phê duyệt ở trên.
- Chưa chạy CI JSR thật, Codex review, merge hoặc release; không chứng minh
  runtime Node 20/22, ERPNext thật hay quyền đọc ledger tại site đích. Không
  publish, nâng version hoặc thay dependency. Kế hoạch vẫn IN_PROGRESS.

## Bàn giao fixture sau khi parent cho tiếp tục

Parent cho phép tiếp tục trên chính base hiện tại, không tích hợp nhánh 017 chưa
được duyệt. Source fixture/host được lưu ở
`bbfd235f010709c087c4832aef1f3f7d02146747`. Executor không thao tác Browser hoặc
host Vite đang chạy. Các scenario 007/009 giữ nguyên; smoke của stock vẫn có
`ITEM-LOCAL`/`Local Warehouse` và ledger tương ứng.

Fixture ledger gồm hai mặt hàng, hai kho, sáu dòng hợp lệ cho A/W1 cùng một dòng
đã hủy có giờ mới nhất. Lọc cặp item/kho và `is_cancelled = 0` trước khi sort và
limit. Hai dòng cùng ngày và giờ kiểm tra tie-break bằng name. Các balance của
fixture khớp chuỗi lượng thay đổi và hàng Stock Balance cuối.

`node --test plans/evidence/015/fixture_test.mjs`: 2 đạt, 0 lỗi. Test đọc chính
fixture dùng bởi host, kiểm tra ba cặp có dữ liệu/rỗng, sort ngày/giờ/tên, giới
hạn 1/5/20, loại cancelled, validation và không làm bẩn fixture khi sửa kết quả.
Một thử nghiệm import fixture vào Deno test bị chặn bởi type-only alias Vite
`~/shared/refresh` trong `DoclistData`. Chuyển riêng kiểm tra fixture UI sang
Node 26 hiện có; không sửa alias/import map, không bỏ typecheck UI, không giảm
bốn test Deno cho production helper hoặc các test tool/client.

Gate sau fixture: focused Deno 34/0, UI typecheck exit 0, server check exit 0,
lint 201 file exit 0, fmt 237 file exit 0, build lại đủ 7 viewer và Node bundle,
Node syntax exit 0, full suite 956/0/4. Pack dry-run vẫn 10 file/7 HTML, không
chứa testing host. SHA-256 lock, Stock HTML và Node bundle không đổi so với bảng
core. Đây vẫn là gate local với workaround, không phải CI JSR thật.

### Hướng dẫn Browser cho parent

Luồng cần kiểm: mở stock viewer trong host local, chọn một hàng, kiểm tra Recent
Movements chỉ thuộc cặp item/kho của hàng đó; đổi hàng trong lúc phản hồi cũ còn
chờ không được làm hiện dữ liệu hoặc lỗi của hàng cũ.

Host do parent điều phối, URL dự kiến
`http://localhost:5178/testing/host.html?viewer=stock-viewer&scenario=stock-movements`.
Khi đổi dropdown Kịch bản phải nhấn Đặt lại để áp dụng, không chỉ đổi option.

| Scenario/hàng                   | Kết quả cần kiểm, chưa được executor xác nhận bằng Browser                                                                                 |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `stock-movements`, ITEM-A / W1  | Năm voucher `STE-A-W1-006`, `005`, `004`, `003`, `002` theo thứ tự; lượng -2, +10, +4, +3, +2 Nos; không `CANCELLED` hoặc `001`            |
| `stock-movements`, ITEM-A / W2  | Chỉ `STE-A-W2-001`, +7 Nos                                                                                                                 |
| `stock-movements`, ITEM-B / W1  | Chỉ `STE-B-W1-001`, +3 Nos                                                                                                                 |
| `stock-movements`, ITEM-B / W2  | `No recent movements for this item and warehouse.`                                                                                         |
| `stock-permission`, bất kỳ hàng | Alert `Permission denied for Stock Ledger Entry (local fixture)`, không fallback hoặc bảng lịch sử giả rỗng                                |
| `stock-malformed`, bất kỳ hàng  | Alert `Invalid stock movement row`, không crash hoặc hiện row lỗi                                                                          |
| `stock-race`                    | Chỉ ledger bị giữ; item_get trả ngay. Chọn A/W1 rồi A/W2 hoặc B/W1, trả request mới trước và request cũ sau; chỉ dữ liệu cặp mới được hiện |
| `stock-race`, lỗi muộn          | Sau khi cặp mới hiện thành công, trả lỗi request cũ; cặp mới vẫn không có lỗi cũ                                                           |

Nút giữ phản hồi của host là `Trả kết quả #N` và `Trả lỗi #N`. Trace chứa tool,
arguments, request ID và outcome received/held/released/rejected. Cần thực hiện
race trong timeout 10 giây thật của panel; không nâng timeout để làm fixture dễ
chạy. Với request hiện tại bị lỗi, alert phải được hiển thị. Host từ chối mọi
`erpnext_doc_*` khi viewer là stock; `erpnext_stock_entry_list` không còn
dispatcher cho panel. Trace hợp lệ chỉ có item_get, stock_ledger_list và refresh
stock_balance nếu viewer tự refresh, không operations hoặc Stock Entry list.

Parent còn phải thu URL/title, DOM, console, screenshot và interaction proof
theo Browser gate. Chưa có screenshot/trace Browser 015 trong commit này, chưa
có review độc lập, CI hoặc merge. Hoàn tất executor local không đồng nghĩa kế
hoạch DONE.

## Parent kiểm Browser và gate local, 2026-09-05

Reviewer độc lập đã đọc source core/host ở HEAD
`d1dbba92192071df8926b7b980d240b9e1b5f35c` và kết luận APPROVE, tự chạy focused
34 test, full suite 956 passed, 0 failed, 4 ignored, fixture Node 2 test,
server/UI typecheck, lint, format và diff check. Reviewer chưa chạy Browser.

Parent đã đọc diff và tự chạy server check, lint 201 file, format 237 file, UI
typecheck, build 7 viewer, Node build/syntax, fixture Node 2 test, full suite
956 passed, 0 failed, 4 ignored; pack dry-run có 10 file và đủ 7 HTML. Các gate
local dùng workaround frozen, không thay CI JSR thật. Node mặc định v26.7.0 được
dùng cho lượt này, chưa chứng minh Node 20/22.

Browser chạy Chrome trên bundle Stock HTML SHA-256
`cd0d677911a66fd2d89e53c7b18c342fe555ad7111677c142b7a8c7e20ede7a4`, Node bundle
tại thời điểm build có SHA-256
`9ae3db57b191e00fdc6f84b4f09f4843b84cf8703c110760ef2db1b8d16d451d`. Tab riêng có
tiêu đề `HVGERP Local Test Host`; URL cuối được đọc qua danh sách tab là
`http://127.0.0.1:5178/testing/host.html?viewer=stock-viewer&scenario=stock-race&chart=bar`.
Không kết nối ERPNext hoặc đọc credential.

Tám scenario đạt bằng tương tác Browser thật:

- A/W1 hiện đúng năm voucher 006, 005, 004, 003, 002 với lượng -2, +10, +4, +3,
  +2 Nos; không có voucher 001 hoặc bản ghi cancelled.
- A/W2 chỉ hiện STE-A-W2-001, +7 Nos; B/W1 chỉ hiện STE-B-W1-001, +3 Nos.
- B/W2 hiện thông báo không có chuyển động, không lẫn dữ liệu cặp khác.
- Permission denied hiện alert đúng lý do; payload sai hiện
  `Invalid stock movement row`. Không fallback về lịch sử rỗng hoặc toàn site.
- Đổi A/W1 sang A/W2 khi ledger cũ đang giữ, trả kết quả mới trước rồi cũ sau:
  panel vẫn chỉ hiện A/W2. Trace chứng minh cả hai request đã được released.
- Đổi A/W1 sang B/W1, trả kết quả mới rồi trả lỗi request cũ: panel vẫn hiện
  B/W1, không xuất hiện lỗi cũ. Trace ghi released rồi rejected đúng request ID.

Parent đọc DOM và trực quan cả 8 PNG trong `plans/evidence/015/browser/`.
`browser-trace.json` chứa 15 record và console export rỗng. Trong đó 12 record
thuộc tám scenario đạt; ba record của thử nghiệm race đầu là chẩn đoán lượt
không đạt, không được tính thành bằng chứng PASS. Lượt đầu chờ locator voucher
thiếu tiền tố `Stock Entry` nên timeout; trong thời gian kiểm tra lỗi, request
cũ bị cancelled bởi timeout thật. Parent reset host và chạy lại với locator
đúng, ghi cả hai phản hồi released trong lượt đạt. Không sửa hoặc tăng timeout
của panel. Lượt đổi item/lỗi muộn cũng hoàn tất trong timeout thật.

Trace chỉ có `erpnext_item_get`, `erpnext_stock_ledger_list` với item/kho và
limit 5, cùng `erpnext_stock_balance` do auto-refresh. Không gọi operations hoặc
stock_entry_list. Console lấy sau lượt cuối với mức warn/error, limit 100: không
có record. Đây là bằng chứng local fixture, không chứng minh quyền ledger trên
site production. Chưa CI, Codex hoặc merge mục 015.

## Review artifact và tích hợp trước PR

Main `1751d5ae3d641a02897007a4405b020dad4c1352` được merge sạch vào
`3f43e3fbc6ab438d9b532efe585de4ff00491ace`, tree
`ae02edfccd4c04596b7cd6ddc90a4ebd8f847486`. Source UI, inventory và client test
của 015 không đổi; parent kiểm diff rỗng trên các phần này. Stock HTML vẫn có
SHA-256 `cd0d677911a66fd2d89e53c7b18c342fe555ad7111677c142b7a8c7e20ede7a4`, nên
artifact Browser phía trên gắn với đúng source/bundle sau tích hợp.

Reviewer độc lập `/root/goal_execute_011` kết luận APPROVE trên HEAD tích hợp,
đã đọc đầy đủ delta, kế hoạch, evidence, cả 15 DOM/trace record và xem cả 8 PNG.
Reviewer tự chạy full suite 1097 passed, 0 failed, 4 ignored, focused 34 test,
server/UI typecheck, lint 204 file, format 244 file, fixture Node 2 test và diff
check. Đếm registry thật: 135 tool, 16 category, inventory 10. Đây là review
artifact của parent, không phải lượt Browser độc lập thứ hai.

Parent tự chạy full suite 1097 passed, 0 failed, 4 ignored, server/UI typecheck,
lint 204 file, format 244 file, Node build/syntax và pack dry-run 10 file/7
HTML. Node bundle sau tích hợp có SHA-256
`2762f4675fc014a6189e3aa8bad088f37fa3228aca9ab24f66eb70cb6c799234`. Browser JSON
có SHA-256 `9d5ca928b9d71e90853cd8791eb3ff96f527ff3f16868635915773ffa55536f6`.
Worktree sạch trước phần ghi báo cáo này. CI JSR thật, Codex và merge vẫn là các
gate còn lại. Không tính việc review local là DONE.

## Tích hợp main sau khi 005 và 019 merge

PR37 mở tại `7f74a481f5213536196c5acee1eff45e1b92c613`; CI
https://github.com/hvgllc/hvgerp-mcp/actions/runs/33956858622 đạt trên đúng
HEAD, JSR thật, full 1097 passed, 0 failed, 4 ignored và release preflight không
publish. Gate này không áp dụng thay cho CI trên revision tích hợp tiếp theo.

Merge main `67a7bc4d777cccced5255b0a43ae648752241f21` có một conflict CHANGELOG.
Executor resolve trong worktree detached, giữ Added 015, Fixed 015 và nguyên văn
ba bullet Fixed 005. Commit `7b1f20b0925d0c644f139248a174f27650309731`, tree
`5d40666e796f65e03023397bb2d818575d74e564`; phần lịch sử từ 3.4.0 trở xuống
không đổi. Source UI/helper/host/fixture/inventory của 015 không đổi.

Reviewer độc lập `/root/goal_execute_011` APPROVE resolution sau khi đọc cả hai
diff và remerge-diff, chạy assertion giữ lịch sử, full suite 1211 passed, 0
failed, 4 ignored, server check, format 248 file và diff check. Executor chạy
thêm focused 34 test, fixture 2 test, lint 206 file, UI typecheck, build 7
viewer, Node build/syntax và pack 10 file/7 HTML, đều đạt. Stock HTML vẫn khớp
SHA-256 của lượt Browser. Parent đọc diff CHANGELOG và xác nhận diff UI rỗng,
fast-forward nhánh PR tới commit đã duyệt rồi tự chạy full suite 1211 passed, 0
failed, 4 ignored. Không force-push hoặc thay nội dung hai bên bằng một bản thay
thế thiếu ghi chú. Cần CI và Codex mới sau push; mục 015 vẫn IN_PROGRESS.

## Tích hợp 017 và Browser vòng 2

Commit tích hợp `49f45838d189877f768d811280c2c037e9ba44bc`, tree
`c30ee1a1ed8f15c97eb5c58d7b558f0934df58aa`, kết hợp main
`95662c3ee38227d718795995889e28be96041b71` và PR37 tại
`65d83be9401d2b92db7ee4a8fb4042c6f50932c9`. Executor resolve đúng ba hunk trong
host/fixtures: hợp nhất scenario, tách dispatch date/stock và giữ nhánh Kanban
trước stock/date. Parent đọc toàn bộ remerge-diff; không bỏ scenario hoặc guard
của bên nào. Reviewer độc lập `/root/goal_execute_011` APPROVE source và
resolution, tự chạy full 1264 passed, 0 failed, 4 ignored, focused 88 test,
runtime boundary 12 test, fixture Node 2 test, server/UI typecheck, lint 206
file, format 251 file và diff check. Executor cũng đạt full 1264, UI build 7
viewer, Node build/syntax và pack dry-run 10 file/7 HTML.

Parent chạy lại Browser thật trên host của chính commit tích hợp này, port 5178,
tab riêng `HVGERP Local Test Host`. Stock HTML SHA-256:
`d6bba7e10be7bcb7d05def93a6e49d49aab09a4e88f7a25c6067020c26e302ac`. Artifact mới
trong `plans/evidence/015/browser-r2/`: 23 PNG và một JSON chứa 48 record, gồm
47 record trạng thái và một diagnostic. JSON SHA-256:
`325cd7d0ebdb2dca74736fdf6a74f984befe184d748997eb7cce0665e11b06d3`. Artifact
vòng 1 vẫn được giữ, không thay thế hoặc sửa để khớp lượt mới.

- Tám ca stock core/race đạt: đúng năm voucher A/W1, đúng A/W2 và B/W1, B/W2
  rỗng trung thực, permission/malformed hiện lỗi, phản hồi cũ sau đổi kho và lỗi
  cũ sau đổi item không ghi đè panel mới. Hai race hoàn tất trong timeout thật,
  trace ghi request được released/rejected đúng thứ tự, không tăng timer.
- Stock initial error và malformed chặn dữ liệu chưa hợp lệ, success phục hồi;
  refresh error giữ dữ liệu đã tải và hiện lỗi inline, success xóa lỗi.
- Invoice, chart, KPI và funnel đều qua initial error rồi success với dữ liệu
  thực của fixture. SO/QTN giữ đúng ngày 2026-09-03/2026-09-04 qua malformed và
  refresh lỗi, sau đó recovery bằng host gửi lại payload.
- Hai service row với item_code null/omitted không gọi Item lookup. Row có mã
  ITEM-LOCAL làm đối chứng, gọi item_get và stock_balance rồi mở detail.
- DocList chỉ kiểm refresh trả ba row. Kanban kiểm dispatcher move/board và
  detail response; đây là smoke host, không chứng minh race của mục 016 hoặc
  thay bằng chứng detail-race 009.

Một diagnostic là timeout locator role alert khi refresh stock thực tế dùng lỗi
inline. Parent ghi nguyên lỗi, chuyển sang locator text đã quan sát và kiểm
recovery; không tính lần timeout thành PASS hoặc sửa sản phẩm cho test. Console
export có 0 error và 1 warning Recharts, không gọi là hoàn toàn sạch. Heuristic
title/party/mutation của SO/QTN và refresh metadata giữ giới hạn baseline ghi
trong evidence 017; lượt này không bấm mutation hoặc tuyên bố đã sửa các giới
hạn ấy. Không kết nối ERPNext, đọc credential hoặc dùng dữ liệu production. Đây
là fixture local, không xác nhận quyền thực tế trên site.

Reviewer `/root/goal_execute_011` đọc toàn bộ 48 record/trace và xem đủ 23 PNG,
APPROVE artifact đúng phạm vi 015/017, xác nhận hash JSON và HTML. Reviewer
không thực hiện một lượt Browser riêng. Parent cũng đã đọc DOM và trực quan toàn
bộ ảnh. Artifact được sao chép nguyên byte sang nhánh PR và kiểm diff thư mục
rỗng.

## Tích hợp sau khi 006 merge

Commit `0fdb66dfde982c5a84a18c911998d455095692fc`, tree
`8b891f4c5f29c0042249d98a9b9a48ad44b9ea42`, merge sạch main
`67896f3208caee923659f1900c399d87e99c403c` và commit 49f phía trên. Reviewer
`/root/review_005` APPROVE, tự chạy full 1323 passed, 0 failed, 4 ignored,
server/UI typecheck, lint 208 file, format 254 file, build đủ 7 viewer, Node
build/syntax, pack 10 file/7 HTML, fixture Node 2 test và 9 test sau build.
Probe analytics 1001 row qua cả text/structured và ledger panel parser đều đạt.
Registry có 135 tool, analytics 17, inventory 10.

Bảy HTML cùng host.ts/fixtures.ts khớp nguyên byte với source Browser 49f, nên
artifact vòng 2 vẫn áp dụng cho phần UI/host sau tích hợp 006. Node bundle mới
có SHA-256 `54ea48785a30f4f7aa602d8c2050a2880ce93e69d6a7395335a8ac5af26a1fdb`.
Parent tự chạy full 1323 passed, 0 failed, 4 ignored rồi fast-forward nhánh 015
tới commit tích hợp đã duyệt. Các gate local dùng workaround frozen, không được
tính thay JSR thật. CI của 65d đã đạt 1211 test và preflight tại
https://github.com/hvgllc/hvgerp-mcp/actions/runs/33957572412 nhưng không áp
dụng thay cho HEAD sắp push. Cần CI và Codex mới trên đúng HEAD mới nhất trước
merge; mục 015 vẫn IN_PROGRESS.

## Sửa finding PR37: tổng số tool trong tài liệu kiến trúc

Finding Codex `3940252615`, review `5120816523`, áp dụng đúng HEAD
`cdad59af2f104d0a09c9ee6e7f83e554c66041a6`. Parent đã đọc finding; executor tự
đối chiếu tài liệu với registry trước và sau sửa. Worktree ban đầu sạch, HEAD
khớp revision được review.

Chỉ sửa số tổng trong câu giới thiệu `docs/architecture.md` từ 129 thành 135.
Giữ nguyên tiếng Anh của file và toàn bộ 16 dòng số lượng theo nhóm. Không đổi
source, UI, host, artifact Browser hoặc plan index.

Assertion tạm chạy bằng
`deno eval --config deno.nojsr.json --sloppy-imports --frozen`, import
`allTools`, `toolsByCategory` và `assertEquals`. Đọc 16 dòng
`*.ts # N ... tools` trong tài liệu, so tập tên nhóm với registry, so từng số
lượng nhóm, rồi so tổng các dòng và tổng trong câu giới thiệu với
`allTools.length`. Không tạo test file hoặc gọi ERPNext.

- Red trước sửa: exit 1; 16 nhóm khớp, tổng các dòng và registry đều 135 nhưng
  câu giới thiệu là 129. Assertion cuối thất bại đúng `129 != 135`.
- Green sau sửa, cùng assertion: exit 0; 16 nhóm khớp, tổng các dòng, registry
  và câu giới thiệu đều 135.
- Format check đầu tiên: exit 1 do wrap dòng evidence mới. Đã format lại hai
  file rồi chạy `deno fmt --check docs/architecture.md plans/evidence/015.md`:
  exit 0.
- `git diff --check`: exit 0. Đã tự đọc diff, chỉ hai file tài liệu/evidence
  được thay đổi.

Đây là kiểm tài liệu local dùng workaround frozen, không phải CI JSR mới. Không
chạy lại full suite, Browser hoặc build vì source và artifact không đổi. Fresh
review, push, phản hồi finding và CI trên HEAD mới thuộc parent.

## Sửa ba finding PR37: lỗi từng phần, Item GET và ledger cache

Lượt này xử lý finding `3940311861`, `3940311864`, `3940311865` thuộc review
`5120885574` trên revision `07405f6c65`. Parent đã đọc và xác minh finding trước
dispatch. Base tích hợp sau main 013:
`4b29d9f019d5c25e8c6e4469ece818e374eab79a`, tree
`38574ae838d48a3a8f7ac13363d259563fbc0240`. Executor tự kiểm worktree sạch và
diff UI/inventory rỗng từ revision được review tới base này. Source sửa:
`3f6fec5f8b9d7544dbad78e58080d26dcb623ee3`.

Đã đọc đầy đủ plan 015 ở root, evidence hiện hữu, skill improve execute,
frontend-testing-debugging và các quy tắc React liên quan effect/dependency.
Executor được parent dispatch riêng, không tự delegate. Chỉ sửa panel, helper
stock-movements và test, inventory và test, docs/tools và evidence này. Không
sửa cache/client, public schema, mutation handler, host/fixtures, dependencies,
version, root plan/index hoặc artifact Browser vòng 1/vòng 2.

### Hành vi đã sửa

- `loadStockDetails` là helper điều phối không phụ thuộc React, nhận callback
  gọi tool và kiểm identity. State và lỗi của Item/ledger tách riêng; Item thành
  công được công bố ngay khi ledger còn chờ. Ledger 403/malformed không xóa
  Name/Group/UOM/Std Rate đã đọc; Item lỗi vẫn tiếp tục thử ledger, không biến
  lỗi thành lịch sử rỗng. Item payload không phải object bị báo lỗi riêng.
- Panel dùng trực tiếp helper, mỗi effect có cancellation guard; render kiểm
  identity gồm item và kho. Phản hồi/lỗi muộn không cập nhật state cặp mới. Giữ
  timeout 10 giây cho mỗi lời gọi, không hủy write hoặc đổi thao tác điều hướng.
  Hai lời gọi giờ nối tiếp: trường hợp Item chậm có thể trì hoãn bắt đầu ledger,
  và hai timeout liên tiếp có thể kéo dài gần 20 giây. Không hứa latency như
  Promise.all cũ; khi Item hoàn tất, thông tin hoặc lỗi Item được hiện trong lúc
  chờ ledger.
- Đọc Item trước ledger để `resolveItem` dùng cache đã có. Test gọi handler thật
  qua `FrappeClient` và `MemoryCache`, đếm tại `fetch` của ERP Item endpoint:
  cold và expired đều một GET mới. Không đồng nhất số MCP call với ERP request.
  NoopCache vẫn hai Item GET, kết quả đúng. Không thêm bypass resolution hoặc
  promise khử GET cho cache tắt, TTL bằng 0/hết hạn giữa hai call, cache khác
  nhau hoặc entry bị thu hồi.
- Ledger `client.list` truyền `{ skipCache: true }` ở tham số thứ ba. Không mở
  rộng invalidation. Regression làm nóng cache ledger, gọi các handler generic
  submit/cancel Stock Entry hiện hữu với client thật, rồi đọc ledger lại ngay:
  có row mới sau submit, không còn row đã hủy sau cancel, tổng ba HTTP ledger
  reads cho ba lần đọc. Mock fetch kiểm đủ filter item/kho/is_cancelled=0; không
  sleep TTL, không giả kết quả test này là hành vi database ERP production.
- `docs/tools.md` ghi rõ giới hạn cache, kết quả từng phần và rằng lần đọc mới
  không phải snapshot nguyên tử hay tự cập nhật panel đang mở.

### Red và green thực chạy trong lượt này

Để kiểm hồi quy bằng hành vi thay vì chỉ source-regex, trước sửa đã trích luồng
Promise.all/parse ledger trước Item của panel cũ thành helper thử nghiệm. Helper
này chưa được nối vào panel ở bước red, giữ nguyên thứ tự xử lý/lỗi cũ; không
tuyên bố đã chạy component Browser cũ. Sau red, sửa helper và nối panel vào
chính helper được kiểm thử.

Lệnh dưới đây thêm `--config deno.nojsr.json --sloppy-imports --frozen`:

- Red
  `deno test --allow-all src/ui/shared/stock-movements_test.ts
  src/tools/inventory_test.ts`:
  18 passed, 3 failed đúng ba finding. Item data thực tế null thay vì bốn trường
  thành công; số Item GET cold/expired thực tế `[2, 2]` thay vì `[1, 1]`; ledger
  sau submit còn cache rỗng thay vì SLE-NEW. Không có lỗi import/typecheck bị
  tính làm red.
- Green cùng lệnh và ba regression đầu: 21 passed, 0 failed.
- Sau bổ sung coverage, focused gồm hai file trên, `src/client_test.ts` và
  `src/runtime-boundary_test.ts`: 62 passed, 0 failed. Trong đó có tám ca late
  Item/ledger success/error khi đổi item hoặc kho; pending ledger vẫn giữ Item,
  Item lỗi permission/transport/malformed vẫn giữ ledger, cả hai lỗi tách biệt,
  cancel trước khi bắt đầu không gọi tool; bốn test parser/request cũ giữ
  nguyên. Đây không phải chạy lại tám scenario Browser trước đây.

### Gate local sau sửa

Workaround theo tài liệu root `docs/jsr-403-workaround.md`: executor so config
ngoài imports với deno.json bằng deep equality, so vendor với cache npm nguyên
bản `@casys/mcp-server@0.25.0` bằng `diff -qr`, đều đạt. Lock giữ SHA-256
`f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`. Không cài
dependency mới; dùng UI dependencies đã có và Node build với npm offline.

| Lệnh/gate                                                              | Kết quả thực chạy                              |
| ---------------------------------------------------------------------- | ---------------------------------------------- |
| `deno test --allow-all src/` với workaround frozen, trước và sau build | Cả hai lượt 1363 passed, 0 failed, 4 ignored   |
| `deno check mod.ts server.ts` với workaround frozen                    | exit 0                                         |
| `deno lint --config deno.nojsr.json`                                   | exit 0, 210 file                               |
| `deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/`    | exit 0, 258 file                               |
| `npm --prefix src/ui run typecheck`                                    | exit 0                                         |
| `node --test plans/evidence/015/fixture_test.mjs`                      | 2 passed, không sửa fixture                    |
| `node src/ui/build-all.mjs`                                            | exit 0, đủ 7 viewer                            |
| `npm_config_offline=true bash scripts/build-node.sh`                   | exit 0                                         |
| `node --check dist-node/bin/hvgerp-mcp.mjs`                            | exit 0                                         |
| `npm_config_offline=true npm pack --dry-run --json` tại dist-node/bin  | exit 0, 10 file gồm 7 HTML, không testing host |
| `git diff --check` và staged diff check                                | exit 0; tự đọc toàn bộ source/test/docs diff   |

Artifact build mới, không commit generated output:

- Stock HTML SHA-256:
  `671945fd5a2c4407eaf5a5d0b4f3c37ecdf3f94bf7f5cef9aa4040e221727edf`.
- Node bundle SHA-256:
  `cf9b70a097229084147e594f6d290b8979882d00436547d31ee6b3ad8fff98c1`.

Node build có warning npm về install scripts của esbuild/fsevents chưa được
allowScripts phê duyệt; build và syntax vẫn exit 0. Không đổi policy hoặc
dependency để che warning. Môi trường vẫn Deno 2.9.5/Node 26.7.0, không chứng
minh Node 20/22 hoặc native JSR.

Browser do parent chạy sau review source, executor không mở/dừng host hoặc tab.
Artifact Browser cũ không được suy ra chứng minh partial Item info của bản mới.
Cần kiểm lại permission/malformed có Local Name/Local Group/Nos/15 và alert,
cùng các ca item/kho/race cũ trên HTML mới. Fresh review, Browser, CI JSR thật,
push/reply/merge còn thuộc parent; không tự ghi APPROVE hoặc DONE.

## Review độc lập và Browser R3 sau ba finding

Reviewer `/root/goal_execute_011` APPROVE source tại HEAD
`9d9fd34e07fd1b67e4c05e3f88d68fbf1163a319`, tree
`22a9c79d154d7b0fe65d184a2cd68b66235413b0`, report blob
`69515ef02db5bc9a86e15374702477ef3025eb99`. Reviewer đọc toàn bộ diff bảy
file/evidence và tự chạy focused 62, full 1363/0/4, check, lint 210, fmt 258, UI
typecheck, Node fixture 2, syntax và diff-check. Reviewer đối chiếu phương pháp
red với source cũ nhưng không tự chạy lại helper thử nghiệm red hoặc Browser.
Parent cũng đọc toàn bộ source/test/docs diff và tự chạy full 1363 passed, 0
failed, 4 ignored trên HEAD này.

Parent chạy Browser thật trên host local của worktree 015, dùng đúng Stock HTML
SHA-256 `671945fd5a2c4407eaf5a5d0b4f3c37ecdf3f94bf7f5cef9aa4040e221727edf`.
Không thay host/fixture, timer, DOM hoặc phản hồi qua công cụ ngoài host. Chỉ
thao tác tab QA đã mở, không gọi ERP. Tám PNG `stock-r3-*.png` là ảnh chụp
nguyên bản, parent đã nhìn đủ từng ảnh. File `browser-stock-r3.json` giữ 14 DOM
snapshot nguyên bản và console đã lọc error/warn. SHA-256 trước format:
`376706248f50424a711b72c4f0f3266810c58566057f7d25b080d07bf2110198`.

- Permission và malformed ledger: vẫn thấy Name Local ITEM-A, Group Local Group,
  UOM Nos, Std Rate 15 cùng alert đúng lỗi. Không hiện lịch sử rỗng giả.
- ITEM-A/W1: đúng năm voucher 006/005/004/003/002, lượng -2/+10/+4/+3/+2, không
  có voucher 001 hoặc row cancelled. ITEM-A/W2 chỉ +7; ITEM-B/W1 chỉ +3;
  ITEM-B/W2 hiển thị không có chuyển động và thông tin Item vẫn đúng ITEM-B.
- Race kho: giữ ledger cũ #3 cho A/W1, chọn A/W2 và giữ #5. Trong lúc pending,
  thông tin Item đã hiện cùng Loading recent movements. Trả #5 trước #3, màn
  hình cuối chỉ có A/W2 và voucher +7.
- Race mặt hàng: giữ #3 cho A/W1, chọn B/W1 giữ #5; trả #5 rồi trả lỗi #3. Màn
  hình cuối vẫn B/W1, +3, không nhận lỗi của A. Hai race thực hiện trong timeout
  10 giây, không chờ request hết hạn rồi giả là phản hồi bị loại.
- Console được đọc cuối lượt: 0 error, 1 warning AppBridge nhận ui/initialize
  lần hai lúc 11:00:23 UTC, đã lưu nguyên message. Không suy warning thành lỗi
  source đã sửa hoặc che nó bằng claim console sạch.

R3 chỉ kiểm tám scenario stock và trạng thái pending kể trên. Các viewer khác
không đổi bundle trong bản sửa này; Browser R2 vẫn là bằng chứng lịch sử riêng,
không giả đã chạy lại toàn bộ viewer. Cache/ERP GET và submit/cancel được chứng
minh bởi regression client/cache thật với fetch fixture, không bởi Browser host.
CI và Codex đúng HEAD mới vẫn là gate chờ trước merge.
