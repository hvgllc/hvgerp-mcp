# Chứng cứ thực thi kế hoạch 015

## Phạm vi và mốc nguồn

- Base: `341cba437dba69348b6e11e2c6f599480d5fc212`.
- Core commit: `0afef87a570201817e09a4599ec48b2986101d83`.
- Branch: `advisor/015-stock-item-movement-query`.
- Mục tiêu: Recent Movements chỉ đọc Stock Ledger Entry của mặt hàng và kho đang
  chọn, không gọi Stock Entry toàn site hay generic operations.
- Kiểm drift từ `d2c5305` đến base: chỉ panel có guard kiểu chuỗi từ 007;
  host/fixtures được tạo bởi 007 và mở rộng detail race bởi 009. Giữ nguyên các
  guard và scenario cũ. Worktree sạch trước khi sửa.

Executor đã tự đọc schema ERPNext v15 qua GitHub Contents API tại commit
`1a0bf0bf6c4aeaae5acde90c74b186312f49b95c`:
[Stock Ledger Entry schema](https://github.com/frappe/erpnext/blob/1a0bf0bf6c4aeaae5acde90c74b186312f49b95c/erpnext/stock/doctype/stock_ledger_entry/stock_ledger_entry.json).
Nguồn có `item_code`, `warehouse`, `posting_date`, `posting_time`,
`voucher_type`, `voucher_no`, `actual_qty`, `qty_after_transaction`,
`stock_uom`, `is_cancelled` (Check, mặc định 0), cùng `name` chuẩn của DocType.
Schema có quyền đọc cho Stock User và Accounts Manager. Đây không phải bằng
chứng quyền đọc trên site triển khai cụ thể.

## Hợp đồng đã triển khai

- Tool mới `erpnext_stock_ledger_list` thuộc `inventory`, read-only, không có
  viewer binding riêng. Required: `item_code` và `warehouse`, chuỗi không rỗng;
  `limit` là integer 1 đến 20, mặc định 5. Kiểm tra trước cả resolve và query.
- Resolve Item ID hoặc tên bằng `resolveItem`; warehouse giữ ID chính xác. Query
  cố định 10 trường trong kế hoạch, ba filter `item_code`, `warehouse`,
  `is_cancelled = 0`; sắp xếp ngày, giờ, tên giảm dần. Kết quả `{data: rows}`.
  Handler không nhận DocType/fields/filter/order tùy ý; direct calls có trường
  thừa cũng không thể ghi đè query. MCP schema từ chối trường thừa.
- Panel dùng builder chung với limit 5, parse và kiểm kiểu từng dòng, từ chối
  dòng khác item hoặc warehouse. Hiện voucher, ngày/giờ, lượng thay đổi và UOM.
  Empty ledger có thông báo riêng; lỗi quyền đọc hiện rõ, không fallback.
- Effect giữ hai request độc lập chạy song song, phụ thuộc cả item và warehouse,
  xóa state khi đổi cặp, có cancellation guard và chặn render state thuộc cặp
  cũ. Không thêm package hoặc thay đổi public API của Stock Entry.

## Test và gate core

Môi trường local: Deno 2.9.5, Node 26.7.0. Dùng workaround đã duyệt từ
`docs/jsr-403-workaround.md`: donor 017, vendor npm `@casys/mcp-server@0.25.0`
khớp nguyên bản cache npm qua `diff -qr`; JSON config giống `deno.json` sau khi
bỏ riêng `imports`. Lock giữ SHA-256
`f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`.

Các lệnh Deno test/check dưới đây đều thêm
`--config deno.nojsr.json --sloppy-imports --frozen`.

| Kiểm tra                                                                                                             | Kết quả thực tế                                   |
| -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| Baseline `deno test --allow-all src/tools/inventory_test.ts src/client_test.ts`                                      | 25 đạt, 0 lỗi                                     |
| Red: thêm test tool/validation/permission/inventory-only trước implementation                                        | 25 đạt, 4 lỗi đúng hành vi tool chưa được đăng ký |
| Focused `deno test --allow-all src/ui/shared/stock-movements_test.ts src/tools/inventory_test.ts src/client_test.ts` | 34 đạt, 0 lỗi                                     |
| `deno check mod.ts server.ts`                                                                                        | exit 0                                            |
| `deno lint --config deno.nojsr.json`                                                                                 | exit 0, 200 file                                  |
| `deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/`                                                  | exit 0, 235 file trước evidence này               |
| `npm ci --offline --no-audit --no-fund --prefix src/ui`                                                              | exit 0, cài 208 package theo lock                 |
| `npm --prefix src/ui run typecheck`                                                                                  | exit 0                                            |
| `node src/ui/build-all.mjs`                                                                                          | exit 0, đủ 7 viewer                               |
| `npm_config_offline=true bash scripts/build-node.sh`                                                                 | exit 0                                            |
| `node --check dist-node/bin/hvgerp-mcp.mjs`                                                                          | exit 0                                            |
| `npm_config_offline=true npm pack --dry-run --json` tại `dist-node/bin`                                              | exit 0, 10 file, đủ 7 HTML, không có testing host |
| `deno test --allow-all src/` sau build                                                                               | 956 đạt, 0 lỗi, 4 ignored                         |
| `git diff --check`                                                                                                   | exit 0                                            |

Test mới bao gồm input thiếu/rỗng/sai kiểu, limit null/0/âm/21/phân số/chuỗi/
NaN/Infinity, Item resolution từ tên, lỗi resolution và ledger permission không
fallback, ba cặp item/kho với giới hạn 1/5/20, query overrides không có hiệu
lực, inventory-only client thật và handler map không chứa operations, parser
text/ structured/empty/malformed/cross-item/cross-warehouse.

Trong lúc viết test, một lần typecheck thất bại vì mock `FrappeAPIError` thiếu
tham số body; sửa mock trước khi ghi nhận red hành vi. Một lần happy-path mock
trả GET thành công nhưng kỳ vọng resolve sang ID khác; sửa fixture thành GET 404
rồi exact-name list theo hợp đồng `resolveItem`. Không đổi resolver để làm test
đạt. Không tính hai lần này là regression của code cũ.

Artifact core local, không commit generated output:

- Stock HTML SHA-256:
  `cd0d677911a66fd2d89e53c7b18c342fe555ad7111677c142b7a8c7e20ede7a4`.
- Node bundle SHA-256:
  `9ae3db57b191e00fdc6f84b4f09f4843b84cf8703c110760ef2db1b8d16d451d`.

## Phần còn chờ

### Đồng bộ catalog được parent phê duyệt

Parent đã đối chiếu và cho phép bổ sung đúng năm tool cũ bị thiếu vào hai bảng
`docs/tools.md` và `docs/coverage.md`: `erpnext_gl_entry_list`,
`erpnext_financial_report`, `erpnext_employee_checkin_list`,
`erpnext_attendance_day_get`, `erpnext_attendance_day_fix`. Đây là đồng bộ tài
liệu trong scope, không sửa source Accounting/HR. Heading Accounting 8 và HR 15
được đối chiếu registry thực. Viewer binding căn theo `_meta` từng declaration,
không suy từ category. `attendance_day_fix` ghi rõ mutation, có thể hủy và dựng
lại Attendance đã submit, cần `confirm_cancel_attendance`, lý do và ứng dụng
`hvg_workspace`; không gắn read-only.

- Parent đang điều phối bản sửa 017 chung host/fixtures. Executor 015 chưa sửa
  hai file đó tại mốc core này và không mở/dừng/restart host hoặc Browser.
- Chưa chạy Browser thật cho 015, chưa có screenshot hoặc trace. Build và unit
  test không thay thế Browser gate. Parent sẽ kiểm các cặp item/kho, lỗi quyền,
  empty/malformed, late response và trace inventory-only bằng fixture local.
- Registry thực là 135 tool / 16 category, inventory 10. Bốn catalog đã thêm
  ledger và số đếm; hai bảng cũ được bổ sung năm dòng thiếu như phần đồng bộ
  catalog đã phê duyệt ở trên.
- Chưa chạy CI JSR thật, Codex review, merge hoặc release; không chứng minh
  runtime Node 20/22, ERPNext thật hay quyền đọc ledger tại site đích. Không
  publish, nâng version hoặc thay dependency. Kế hoạch vẫn IN_PROGRESS.
