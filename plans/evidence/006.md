---
review_verdict: APPROVE
plan_id: 006
reviewed_commit: d0b122dbd2273d2ae9e6d480eca96684088acc82
completed_commit: 67896f3208caee923659f1900c399d87e99c403c
reviewed_evidence_blob: 45ab687630f84089a429448e24f8b163a6fea74f
completed_evidence_blob: 45ab687630f84089a429448e24f8b163a6fea74f
---

# Bằng chứng thực thi 006: Tổng hợp đủ trước khi xếp hạng

## Phạm vi và nguồn

- Ngày xác minh: 2026-09-05.
- Base: `67a7bc4d777cccced5255b0a43ae648752241f21`, sau khi 005 đã merge.
- Source: `cf2e43b3459bdd87ff3bce019ad0ac75369166d9`.
- Branch: `advisor/006-complete-analytics-aggregates`.
- Worktree riêng bắt đầu sạch. Đã đọc AGENTS và hướng dẫn workaround tại root,
  đối chiếu diff từ `d2c5305` để giữ company/currency, ownership, report và URL
  budget của 005. Không merge main mới trong lúc thực thi.
- Chỉ sửa 9 file source/test/docs trong scope và bằng chứng này. Parent giữ
  `plans/README.md`; executor không cập nhật index, không push, mở PR hoặc
  merge.

## Thiết kế và ranh giới

`listCompleteAnalytics` đọc trang 1.000 dòng, bổ sung `name` vào fields và
`name asc` vào cuối thứ tự hiện có. Nếu không có order thì dùng
`modified desc, name asc`. Offset tăng đúng 1.000; trang ngắn kết thúc; đúng bội
vẫn dò thêm trang rỗng. Không dùng `sum(...)`, SQL, get_all hoặc đổi quyền
caller. Mỗi trang phải là array không quá kích thước yêu cầu, mỗi dòng có name
hợp lệ và duy nhất. Lỗi trang sau, quyền bị từ chối, trang lặp, dòng sai hoặc
vượt ngân sách đều hủy kết quả, không trả prefix hay đổi lỗi thành 0.

Thêm `listAllDocuments`, `listAllItems`, `listAllBins` trong context thay vì bọc
offset quanh `listScoped` top N cũ. Đường complete kiểm parent/parenttype và
warehouse trên từng trang, đọc hết từng chunk, kiểm trùng name xuyên chunk rồi
giữ thứ tự số/timestamp toàn cục. URL budget 6.000 tính cả fields, filters,
tie-breaker và offset lớn nhất. Chunking kiểm trước mọi request, không bỏ ID quá
dài. Các test ownership/URL cũ của 005 vẫn giữ nguyên; fixture Item Price được
thêm name vì truy vấn mới yêu cầu trường này.

Ngân sách không phải một con số duy nhất cho toàn tool:

- Các complete document/child/Bin cùng một context chia sẻ tối đa 1.000 request
  và 100.000 dòng qua mọi chunk và nhánh đồng thời.
- Parent/Warehouse discovery giữ guard riêng 100.000 name của 005, tối đa 101
  trang mỗi lần; Warehouse được memo trong context. Company GET và lựa chọn
  Company nằm ngoài ngân sách complete.
- Item Price, Lead, các cửa sổ KPI orders và các đầu vào stock không có company
  context dùng cùng guard nhưng riêng từng lượt đọc đầy đủ. Một lượt đơn lẻ đạt
  100.000 dòng cần 101 request; dòng 100.001 làm toàn lượt đọc thất bại.
- Item UOM lookup kiểm đủ/duy nhất như 005 và từ chối hơn 100.000 ID hoặc hơn
  1.000 chunk trước khi gửi request. Accounts Receivable là một report đồng bộ,
  không phân trang local; response hơn 100.000 dòng bị từ chối toàn bộ.
- Hai đường chọn/hiển thị không tổng hợp vẫn dùng top N cũ: radar tự chọn 4 Bin
  và scatter fallback hiển thị các Bin. Chúng không được mô tả là tổng toàn site
  hoặc là complete aggregate; chi phí theo số chunk ownership như 005.

Không có atomic snapshot giữa các trang. Các lượt đọc xảy ra nối tiếp trong thời
gian tool chạy, có thể dùng cache TTL hiện có. `generatedAt` nếu có chỉ là thời
điểm tạo output. Duplicate detection phát hiện được một số dịch chuyển, không
chứng minh không có omission khi ERP thay đổi đồng thời. Ngày, draft, ngữ nghĩa
ratio giữa các tập độc lập và đơn vị tiền/số lượng của 005 giữ nguyên.

## Ma trận toàn bộ analytics và lookup

| Tool hoặc nhánh                   | Cap đầu vào cũ và tác động                             | Nguồn sau sửa, fixture và kết quả                                                                                                  |
| --------------------------------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| stock_chart                       | Bin bị cắt theo top N trước cộng kho; Item group 1.000 | Đọc đầy đủ cả Item group và Bin; 1.001 Item và 1.003 Bin; B ở hai kho 6+6 thắng A=10, top 1 là B=12                                |
| sales_chart customer              | 500 Sales Invoice                                      | 1.001 invoice, tổng 10.010 VND, company giữ nguyên                                                                                 |
| sales_chart status                | 500 Sales Invoice                                      | 1.001 invoice, tổng status 10.010; không đổi draft/status filters                                                                  |
| sales_chart item                  | 500 Sales Invoice Item                                 | 1.001 child rows, tổng item 10.010; giữ ownership và parenttype                                                                    |
| revenue_trend total/customer      | 1.000 Sales Order                                      | Đọc đủ cửa sổ trước chia tháng và top 5 customer; fixture tới 5.001 đơn, tổng 10.002; nhánh customer/currency cũ vẫn qua           |
| order_breakdown pie/donut/stacked | 500 Sales Order                                        | Đọc đủ trước tổng theo customer/status và top N; fixture tới 5.001 đơn, tổng 10.002; regression stacked giữ nguyên                 |
| revenue_vs_orders                 | 500 Sales Order                                        | Đọc đủ trước sum/count customer; fixture tới 5.001 đơn, revenue 10.002; regression hai axis giữ nguyên                             |
| stock_treemap item/warehouse      | 500 Bin                                                | 1.001 Bin, tổng 5.005 cho cả hai nhánh; top N sau cộng                                                                             |
| product_radar chọn mặc định       | 4 Bin                                                  | Giữ giới hạn chọn phạm vi; schema/guard tối đa 8 item không đổi                                                                    |
| product_radar các chiều           | 100 Bin mỗi item, 500 Sales Order Item                 | 1.001 Bin của A so với một Bin B=1.001; 2.002 child rows chia đều; cả hai series đạt `[100,100,100,100]`                           |
| price_vs_qty giá/qty              | 200 Item Price, 500 Sales Order Item                   | 1.001 giá, item có đơn chỉ nằm ở giá cuối; 1.001 child rows qty=2 cho kết quả x=10, y=2.002                                        |
| price_vs_qty UOM                  | Lookup các item đã chọn                                | Giữ đủ, duy nhất, đúng chunk, currency và UOM; URL ordinary/long/Unicode 200 mã của 005 vẫn qua; 1.001 chunk bị chặn trước request |
| price_vs_qty fallback             | Top N Bin theo stock_value                             | Giữ hiển thị raw Bin, không sum; chỉ fallback khi không có giao điểm giá/đơn, không fallback khi page/UOM/quyền lỗi                |
| kpi_revenue                       | 5.000 Sales Order sáu tháng                            | Fixture 0,999,1.000,1.001,2.501,5.001 đơn; 5.001 đơn cho 10.002, sparkline trước khi cắt tháng                                     |
| kpi_orders                        | 1.000 mỗi cửa sổ                                       | 2.002 đơn tháng này, 1.001 tháng trước: value 2.002, delta 100%; giữ scope caller, không thêm company                              |
| kpi_gross_margin                  | 1.000 child, 500 Bin                                   | 1.001 child và Bin, cost lookup đủ, margin 50%; không đổi cách chọn valuation đầu tiên                                             |
| kpi_outstanding                   | Report sau 005, không còn cap invoice list             | 1.001 ledger rows, tổng 2.002 VND; đúng một Accounts Receivable report                                                             |
| kpi_overdue                       | Report sau 005                                         | 1.001 voucher quá hạn, count 1.001, không đếm dòng tổng/report khác                                                                |
| sales_funnel Lead                 | 500 Lead                                               | 501 Lead caller nhìn thấy, count 501; không tự thêm company                                                                        |
| sales_funnel Opportunity          | 500 Opportunity                                        | 501 cùng company, value 1.503 VND                                                                                                  |
| sales_funnel Quotation            | 500 Quotation                                          | 501 cùng company, value 2.004 VND                                                                                                  |
| sales_funnel Sales Order          | 500 Sales Order                                        | 501 cùng company, value 2.004 VND; giữ ratio giữa tập độc lập                                                                      |
| ar_aging                          | Report sau 005                                         | 1.001 ledger rows, tổng bucket 2.002; site date và company currency không đổi                                                      |
| gross_profit item/customer        | 500 invoice child, 500 Bin, 500 invoice lookup         | 1.001 mỗi tập; parent `INV-1000` vẫn map đúng Customer; revenue 10.010, margin 50%; giá vốn hiện tại vẫn là ước tính               |
| profit_loss                       | Report chuẩn; Account lookup N bằng số root account    | Không sửa thuật toán/report. Lookup thiếu account đã lỗi rõ, không có prefix total. Toàn bộ regression report/summary/currency qua |
| Company selection                 | 21 Company để phát hiện mơ hồ                          | Đây là chọn scope, không phải aggregate; giữ lỗi khi nhiều company và không truyền company                                         |
| Parent/Warehouse ownership        | 1.000 tên mỗi trang, guard 100.000 của 005             | Giữ toàn bộ test 005; thêm complete child/Bin 3.003 dòng qua từng chunk, URL thật và Unicode, offset/page size thực                |

Không có đường purchase child trong analytics hiện tại. Không thêm một truy vấn
mới để khớp tên ví dụ tổng quát trong kế hoạch.

## Red/green và chi phí đo được

- Trước sửa source: chạy `analytics_test.ts --filter complete`, 1 passed và 7
  failed. KPI 1.001 bị trả 1.000, breakdown 999 đơn bị tổng 1.000 thay vì 1.998,
  funnel 501 mỗi stage bị 500; stock chọn sai item vì lookup bị cắt.
- Lần chạy sau migration đầu: 154 passed, 20 failed vì fixture cũ thiếu name dù
  API mới yêu cầu name. Đã thêm identity trong fixture UI-shape cũ và các
  fixture liên quan, không nới validation production hoặc bỏ test lỗi.
- Lần test Unicode đầu: 188 passed, 3 failed vì tên chưa đủ dài để tạo nhiều
  chunk. Tăng độ dài fixture từ 35 lên 60 lần chuỗi Unicode; giữ kiểm URL thật
  và kiểm nhiều chunk, không bỏ assertion.
- Cuối cùng: 205 test trọng tâm passed, 0 failed. Toàn suite 1.261 passed, 0
  failed, 4 ignored. So với baseline 1.202: thêm 59 test.

| Fixture                  | Request/row thực tế                                          | Kết quả                                                               |
| ------------------------ | ------------------------------------------------------------ | --------------------------------------------------------------------- |
| Helper 0 dòng            | 1 request, 0 dòng                                            | Tập rỗng hợp lệ                                                       |
| Helper 999/1.000/1.001   | 1/2/2 request, đúng số dòng tương ứng                        | Đúng bội 1.000 phải có trang rỗng                                     |
| Helper 2.000/2.501       | 3/3 request                                                  | Không cắt sau hai trang                                               |
| Helper 100.000           | 101 request, 100.000 dòng                                    | Trả toàn bộ sau trang cuối rỗng                                       |
| Helper 100.001           | 101 request, 100.001 dòng nhận                               | Lỗi row safety limit, không trả prefix                                |
| Stock 1.003 Bin, top 1   | 2 request ở offset 0/1.000                                   | Cộng đủ rồi mới xếp hạng                                              |
| Scatter giá cuối         | 2 Item Price + 1 parent + 2 child + 1 Item, thêm Company GET | 6 list request, 7 tổng request; 1.001 giá và 1.001 child, qty 2.002   |
| Radar A/B                | 1 Warehouse + 3 Bin + 1 parent + 3 child, thêm Company GET   | 8 list request, 9 tổng request; không N+1 theo từng dòng              |
| KPI count hai tháng      | Offset 0/1.000/2.000 rồi 0/1.000                             | 5 request, 3.003 dòng, delta 100%                                     |
| Treemap 1.001 chunk rỗng | 1 Company GET + 2 ownership pages + 1.000 Bin requests       | Lỗi trước Bin request 1.001; tổng 1.003 request, không trả chart rỗng |
| Item UOM 1.001 chunk     | 0 request                                                    | Chặn trước đọc vì đã biết vượt budget                                 |

Chi phí trên là mock/fetch giả với `limit` và `limit_start` được thi hành thật,
không phải phép đo latency hay tải trên ERPNext production. Test actual URL dùng
FrappeClient thật nối tới fetch giả, kiểm page size 1.000 sau normalizeLimit.

## Các gate đã chạy

Môi trường: Deno 2.9.5, Node 26.7.0, aarch64 macOS. Không tuyên bố đã chạy Node
20/22, Browser hoặc ERPNext production cho mục này.

Local theo workaround đã được phép, không phải native JSR gate:

- Copy nguyên config/vendor/lock từ integration-005; vendor diff với npm cache
  pristine casys 0.25.0 không có sai khác; config ngoài imports khớp deno.json.
- SHA-256 lock giữ nguyên:
  `f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`.
- Test dùng
  `deno test --config deno.nojsr.json --sloppy-imports --frozen
  --allow-all`
  với bốn file analytics/context/pagination/query-report: 205/0.
- `deno check --config deno.nojsr.json --sloppy-imports --frozen mod.ts server.ts`:
  PASS.
- `deno lint --config deno.nojsr.json`: PASS, 205 file.
- `deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/`: PASS, 245
  file trước thêm evidence.
- `deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/`:
  1.261 passed, 0 failed, 4 ignored.
- `npm ci --offline --no-audit --no-fund --prefix src/ui`: PASS, đúng UI lock,
  208 package; không chỉnh dependency. Npm có cảnh báo install-scripts nhưng
  không đổi cấu hình approval hệ thống.
- `npm --prefix src/ui run typecheck`: PASS.
- `node src/ui/build-all.mjs`: PASS, đủ 7 viewer.
- `npm_config_offline=true bash scripts/build-node.sh`: PASS, casys 0.25.0; chạy
  lại sau sửa source cuối.
- `node --check dist-node/bin/hvgerp-mcp.mjs`: PASS.
- `npm pack --dry-run --json --offline` tại package publishable: PASS, 10 file,
  có 7 HTML; không publish.
- SHA-256 bundle:
  `da72878ff0b321a5e5e0477425d28f2e70dc2d85f96178fa2b82cd21b84cca35`.
- Đã tự đọc source diff, `git diff --check` PASS. Chưa có independent review
  hoặc Codex PR review cho source này; đó là gate parent phải thực hiện.

Native JSR/CI Test, kiểm tích hợp main mới, PR review và merge chưa được thực
hiện bởi executor. Không chạy Publish, không bump version, không sửa dữ liệu
ERPNext hay thay dependency. Bằng chứng này chưa phải trạng thái DONE của goal.

## Review độc lập và tích hợp main 017

Reviewer độc lập APPROVE source `cf2e43b3459bdd87ff3bce019ad0ac75369166d9` và
HEAD evidence `e20bb96ae5ae6351e29b5f8f1243fcb720e65832`, sau khi đọc đủ kế
hoạch, ma trận 17 tool, source/test/docs diff. Reviewer tự chạy 217 test trọng
tâm cùng runtime boundary, server check, lint/format/diff. Parent tự đọc source
diff, chạy 205 test trọng tâm và full suite 1261 passed, 0 failed, 4 ignored;
server check và diff đạt.

Hai probe riêng của reviewer xác nhận Warehouse chỉ đọc một lần trong radar tám
nhánh qua 126 chunk; đúng 1000 Bin request rồi lỗi, chưa đọc child. Ba document
stream 35000 dòng mỗi stream cùng bị từ chối khi vượt ngân sách chung. Do
response đang bay, probe nhận thực tế 103000 dòng trước khi tất cả reject:
100000 là trần dữ liệu được chấp nhận, không phải hard cap truyền mạng hoặc bộ
nhớ. Không có stream nào trả tổng prefix.

Tích hợp main `95662c3ee38227d718795995889e28be96041b71` không conflict tại
commit `ef5f6f52271abe8cdadee6ef78557bd43f8904e1`, tree
`bed7581654966d9ce789d700b3ae33a8397a5ab7`. Reviewer APPROVE cây kết hợp:

- Full Deno 1314 passed, 0 failed, 4 ignored; parent tự chạy lại cùng kết quả.
- Server check, lint 205, format 249, UI typecheck và UI build 7 viewer đạt.
- Node build offline, syntax và pack dry-run 10 file/7 HTML đạt; thêm 9 test
  handshake/resource sau build đạt.
- Probe 17 handler với 30 trường hợp và 60 envelope text/structured được parser
  017 chấp nhận, gồm 12 dạng chart, KPI và funnel; giữ dữ liệu khi lỗi và phục
  hồi đạt.
- Cả 7 HTML giống từng byte với artifact 017 đã review. Không chạy Browser mới
  vì 006 không sửa UI; đây không phải bằng chứng ERPNext production.
- Node bundle SHA-256 giữ
  `da72878ff0b321a5e5e0477425d28f2e70dc2d85f96178fa2b82cd21b84cca35`.

Tất cả gate Deno local vẫn dùng workaround frozen đã nêu, Node 26.7.0. Chưa có
native JSR CI hoặc Codex review trên PR 006 tại thời điểm ghi đoạn này.

## Hoàn tất trên main

[PR38](https://github.com/hvgllc/hvgerp-mcp/pull/38) đã merge lúc
2026-09-05T09:51:48Z tại `67896f3208caee923659f1900c399d87e99c403c`, đúng HEAD
`d0b122dbd2273d2ae9e6d480eca96684088acc82`. Parent đã fetch và kiểm
`git diff --exit-code` giữa HEAD được duyệt và merge: exit 0; tree main
`918faf81e30994e28d40421111343f66c280b97c`. Source của tree tích hợp đã kiểm
1314 test giữ nguyên; phần khác biệt so với commit tích hợp chỉ là báo cáo này.

[CI Test33958823021](https://github.com/hvgllc/hvgerp-mcp/actions/runs/33958823021)
đạt trên đúng HEAD, tải JSR casys0.25.0 thật, 1314 passed/0 failed/4 ignored ở
cả Tests và release preflight; preflight OK, không publish.
[Codex5550996783](https://github.com/hvgllc/hvgerp-mcp/pull/38#issuecomment-5550996783)
sạch và ghi reviewed commit d0b122dbd2. Parent đọc nguyên comment và helper:
clean_comment=true, findings=[], findings_error=false. Không dùng clean của
commit khác hoặc chỉ dùng workflow xanh để thay review.
