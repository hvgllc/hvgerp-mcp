# Bằng chứng 010: Bảo vệ chuyển thẻ bằng trạng thái mới và modified

- Trạng thái kế hoạch: IN_PROGRESS. Executor: REVIEW_READY, chưa review độc lập.
- Base: `99b1fa319590e60730faabdb033a5b48a44e1862`.
- Nhánh: `advisor/010-kanban-conflict-protection`.
- Môi trường: macOS arm64, Deno 2.9.5, Node 26.7.0.

## Phạm vi và đối chiếu

Đã đọc kế hoạch đầy đủ, skill improve/closing-the-loop và hướng dẫn JSR.
`git status --short` ban đầu chỉ có plan 010 untracked do parent cấp. So với
`d2c5305`, chỉ client test trong phạm vi có thêm 219 dòng regression của 004; ba
adapter chưa đổi. Giữ nguyên phần đã merge, không sửa generic client, handler,
transition, site, dependency hoặc version.

Skill improve execute yêu cầu executor làm trong worktree riêng và parent review
độc lập. Không sửa workspace của người dùng; không push, mở PR, merge hoặc
publish. Plan/index do parent quản lý, không đưa plan untracked vào commit.

## Chứng cứ cơ chế upstream

Đã đọc nguồn primary tại commit Frappe
`755b5cb81fabb431265690fca07f4a8038a5599a` trước khi sửa:

- [api/v1.py, update_doc](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/api/v1.py#L46):
  PUT đọc document với `for_update=True`, cập nhật dữ liệu request, gọi save.
- [document.py, _save](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/model/document.py#L353):
  kiểm quyền rồi đặt timestamp và kiểm phiên bản trước `db_update`.
- [set_user_and_timestamp](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/model/document.py#L553),
  [check_if_latest](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/model/document.py#L789),
  [load_doc_before_save](https://github.com/frappe/frappe/blob/755b5cb81fabb431265690fca07f4a8038a5599a/frappe/model/document.py#L1076):
  token được giữ trong `_original_modified`, đối chiếu với bản đọc khóa và báo
  `TimestampMismatchError` khi khác nhau.

Đây là kiểm chứng source tại pin, không xác nhận phiên bản hoặc custom hook trên
production. Không truy cập ERPNext thật, không chứng minh database lock bằng
fixture. Đã ghi giới hạn này trong `docs/erpnext-quirks.md`.

## Red và green

Lệnh trọng tâm:

```bash
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/kanban/adapters/ src/api/frappe-client_test.ts
```

1. Trước sửa adapter: exit 1, 69 passed / 27 failed. Cả ba ca stale cache chỉ
   quan sát một GET thay vì hai; cả ba race không reject. Missing-token và kỳ
   vọng payload mới đều đỏ. Một lần soạn test ban đầu lỗi tên thuộc tính error
   đã sửa trước khi chạy được regression; không tính lỗi type là red.
2. Chỉ thêm skipCache: exit 1, 96 passed / 24 failed sau bổ sung 24 ca giữ lỗi
   và transition. Stale cache đã xanh, race GET/PUT vẫn đỏ vì không reject;
   missing-token vẫn trả thành công. Đây là bằng chứng GET mới chưa đủ.
3. Thêm guard token và gửi modified: exit 0, 120 passed / 0 failed.
4. Chạy riêng `src/kanban/adapters/`: exit 0, 27 passed / 0 failed.

Ma trận HTTP trong client test dùng `FrappeClient` và `MemoryCache` thật, chỉ
thay fetch và khôi phục trong finally. Hai lần đọc ban đầu tạo và chứng minh
cache hit. Server giả đổi trạng thái trước move hoặc sau khi chụp snapshot GET
mới, không dùng sleep. Nếu PUT không có token, fixture cho ghi để test thực sự
phát hiện thiếu guard, không tự bảo vệ thay adapter.

Cho mỗi Task/Opportunity/Issue đã kiểm: cache cũ bị bỏ qua; race mang token cũ
và giữ nguyên trạng thái writer khác; success gửi đúng status và timestamp, xóa
cache get/list, lần đọc sau quan sát timestamp mới; GET/PUT lỗi mạng, 403, 500
giữ FrappeAPIError; cột nguồn/đích sai không PUT. Mỗi adapter kiểm undefined,
null, chuỗi rỗng, chỉ khoảng trắng và số đều không PUT. Mapping và các
transition cũ giữ nguyên.

## Môi trường phụ thuộc và build

Sao chép artifact workaround từ donor 011, không sửa vendor. `diff -qr` vendor
với donor: exit 0. Kiểm JSON ngoài imports khớp hoàn toàn deno.json. Lock trước
và sau gate giữ SHA-256:
`f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`. Không chạy
deno task với workaround, không bỏ typecheck hoặc test.

Các lệnh đã đạt exit 0:

```bash
npm --prefix src/ui ci --offline --no-audit --no-fund
deno check --config deno.nojsr.json --sloppy-imports --frozen mod.ts server.ts
deno lint --config deno.nojsr.json
npm --prefix src/ui run typecheck
node src/ui/build-all.mjs
npm_config_offline=true bash scripts/build-node.sh
node --check dist-node/bin/hvgerp-mcp.mjs
deno fmt --check --config deno.nojsr.json --ignore=.nojsr-vendor/
git diff --check
```

Lint: 198 files. Full fmt cuối: 234 files gồm evidence và plan untracked; lần
trước chỉ vướng wrap dòng plan, source/docs đều đạt. UI build đủ 7 viewer. Node
build xác nhận casys 0.25.0; `npm pack --dry-run --json` trong `dist-node/bin`
đạt 10 files, đủ 7 `ui-dist/*/index.html`, không publish. Một phép kiểm phụ dùng
nhầm đường dẫn `src/ui/dist` trong package báo ENOENT; đã sửa phép đọc sang
`ui-dist` và xác nhận đúng 7 viewer, không thay artifact. Npm có cảnh báo
install-scripts, các build vẫn exit 0; không approve script hoặc nâng
dependency.

Bundle Node SHA-256:
`a16b0354f875b4afcdd75a3fb0f3698b4b97f7adacff4843bb354182eefafac7`. Kanban HTML
SHA-256: `4f98b95408cbda98d64e9c4d02d801ef88dc1680a0d2f0a2b1fc2f805d21c047`.

## Full suite và giới hạn còn lại

Full suite chạy sau khi UI/Node build kết thúc:

```bash
deno test --config deno.nojsr.json --sloppy-imports --frozen --allow-all src/
```

Lần đầu: exit 1, 944 passed / 3 failed / 4 ignored. Ba happy-path trong
`src/tools/kanban_test.ts` thiếu modified, guard mới không cho ghi. Đã STOP và
báo parent vì file ngoài scope ban đầu. Parent đọc ba ca và phê duyệt mở scope
hẹp: thêm modified và assert skipCache trong GET riêng mỗi happy-path, kiểm
payload PUT đầy đủ. Không thay mock mặc định hoặc handler, không nới guard.

Sau sửa fixture được duyệt: exit 0, 947 passed / 0 failed / 4 ignored. Bốn
ignored là integration đọc Customer, Item, DocType Customer và Sales Invoice cần
ERPNext thật, không thêm hoặc bỏ qua test mới. Sau đó check, lint 198, full fmt
234 và `git diff --check` đều exit 0. Đã tự đọc toàn bộ diff và xác nhận chỉ
chín file source/test/docs được duyệt cùng evidence này; artifact build, vendor,
config và lock đều ignored. Không còn blocker local.

Không chạy ERPNext production, Browser/mobile, Docker, Node 20/22 hoặc hosted
CI. Kiểm HTTP giả không thay thế xác minh Frappe đang triển khai. Chưa có review
độc lập hoặc PR/CI/merge; chưa đánh dấu DONE.
