---
review_verdict: APPROVE
plan_id: 020
reviewed_commit: c886e77a844b911eddac28e760fa6059cf08ea83
completed_commit: 7d4546b8d303af5323205c75e6d6b51ba367d3c4
reviewed_evidence_blob: 372dbaf2605d0750302e7baa9f40966c87365112
completed_evidence_blob: 372dbaf2605d0750302e7baa9f40966c87365112
---

# Bằng chứng thực thi kế hoạch 020

## Phạm vi và nguồn

- Base: `b409cbb760c6d68b8cffde435978b13772ec4cbe`.
- Source commit: `a4afe7dc6604b1cd7379d722b0b8f43e35af4953`.
- Branch: `advisor/020-align-tool-schema-boundaries`.
- Đã đọc trọn plan 020 ở root, AGENTS.md, skill improve và closing-the-loop.
  Executor được parent giao triển khai riêng; không tự delegate. Parent giữ
  plan/index/manifest, không cập nhật DONE trong đợt này.
- Worktree sạch trước sửa; đã chạy status, diff stat từ `d2c5305` và diff chưa
  commit cho bốn file trong scope. Đã đối chiếu quote Kanban dòng 111, months
  dòng 338, columns/filter của ba adapter và wrapper company hiện tại. Thay đổi
  trước đó của 005/006/013/014 giải thích được bằng diff lịch sử.
- Chỉ sửa hai source, hai test tương ứng và evidence này. Không đổi adapter,
  analytics-context, helper ngày/phân trang, API client, UI, dependency,
  version, plan/index/manifest hoặc dữ liệu ERP.

## Hợp đồng đã triển khai

| Biên                  | Hành vi                                                                         |
| --------------------- | ------------------------------------------------------------------------------- |
| Issue                 | Open, Replied, On Hold, Resolved, Closed                                        |
| Opportunity           | Open, Replied, Quotation, Converted, Closed, Lost                               |
| Schema status         | Hợp của hai tập trên; description nói rõ Issue/Opportunity và Task không hỗ trợ |
| Direct handler status | Kiểm đúng tập của DocType trước resolveUserInputs và mọi client call            |
| Task                  | Giữ nguyên việc không dùng status làm filter; không thêm tính năng lọc          |
| Schema months         | integer, minimum 1, maximum 60, default 6                                       |
| Direct handler months | Chỉ số nguyên 1..60 hoặc bỏ trống dùng 6; kiểm trước wrapper company            |

Các status thuộc DocType khác, không biết hoặc sai kiểu nay bị handler
Issue/Opportunity từ chối bằng lỗi validation. Union schema không tự phân biệt
DocType; handler chịu trách nhiệm ràng buộc đó. Task giữ nguyên hành vi adapter
cũ, không diễn giải union enum thành khả năng lọc mới.

Months 0, -1, 1.5, 61, NaN, Infinity, chuỗi, null, boolean và array bị từ chối.
NaN/Infinity không phải giá trị JSON hợp lệ, được kiểm qua direct handler. Không
coercion chuỗi thành số hoặc xem null như default. Đầu vào hợp lệ giữ tool name,
response shape, viewer metadata, company/currency, refresh request, ngày,
population và ngân sách complete-read hiện hữu.

Đã kiểm source `companyAnalyticsTool`: nó phân giải company trước khi gọi inner
handler. Vì vậy kiểm months chỉ trong inner handler sẽ vẫn phát sinh Company
read. Helper riêng `revenueTrendTool` giữ nguyên wrapper đó nhưng thêm preflight
ở public handler bên ngoài; input sai không tới wrapper, lookup company/timezone
hoặc cấp phát mảng. Không thay contract dùng chung của 005.

## TDD và ma trận kiểm thử

Mọi lệnh Deno test/check bên dưới thêm
`--config deno.nojsr.json --sloppy-imports --frozen`.

1. Thêm test trước sửa production. Lượt đầu có lỗi TypeScript do kết quả
   assertRejects là unknown; đã thêm assertion Error. Không tính lỗi biên dịch
   này là behavioral red.
2. Chạy
   `deno test --allow-all --filter boundary src/tools/kanban_test.ts
   src/tools/analytics_test.ts`
   trên source cũ: 19 passed, 39 failed, 144 filtered out, exit 1. Issue On
   Hold/Resolved bị schema chặn; input status và months sai đi tới client stub;
   schema months chấp nhận 0; mô tả Task chưa nói không hỗ trợ. Control Task
   filter thực tế vẫn đúng. Không khẳng định lượt này tái hiện RangeError: stub
   chặn ở client call xảy ra trước đó.
3. Sau sửa source, cùng lệnh đạt 58 passed, 0 failed, 144 filtered out. Filter
   gồm 55 test mới và ba test date-boundary có sẵn.
4. Chạy trọn hai file đạt 202 passed, 0 failed. Không xóa, giảm assertion hoặc
   thay expected của test cũ để đạt gate.

| Nhóm mới                        | Số test | Assertion chính                                                                                                |
| ------------------------------- | ------: | -------------------------------------------------------------------------------------------------------------- |
| Status hợp lệ Issue/Opportunity |      11 | Framework SchemaValidator chấp nhận và query có đúng status filter                                             |
| Status sai theo DocType/kiểu    |      15 | Không gọi list/get/callMethod, kể cả có user filter `me` cần phân giải                                         |
| Task và status schema sai       |       2 | Task chỉ giữ project filter; schema từ chối unknown/non-string                                                 |
| Months schema                   |       1 | Default/1/6/60 hợp lệ; 0/-1/1.5/61/string/null/boolean/array không hợp lệ                                      |
| Months direct handler sai       |      20 | Mười giá trị sai, mỗi giá trị với company bỏ trống và company cụ thể; zero client calls và không RangeError    |
| Months hợp lệ                   |       6 | Default/1/60 × total/customer; đúng độ dài, tổng 12 EUR, company pinned, Sales Order company/docstatus filters |

SchemaValidator là validator của framework hiện dùng trong repo, không phải
validator giả trong test. Test months sai dùng raw mock client, không dùng
company helper có thể che mất lookup. Các test hợp lệ xác nhận wrapper vẫn thêm
currency EUR và company vào refresh request; các test hồi quy hiện hữu kiểm tiếp
ngày, quyền, population draft và pagination.

## Môi trường và gate local

Runtime thực chạy: Deno 2.9.5, TypeScript 6.0.3, Node 26.7.0. Không chứng minh
Node 20/22 trong đợt local này.

Theo `docs/jsr-403-workaround.md`, sao nguyên ignored config/vendor/lock từ
worktree 014. Deep equality config ngoài imports với deno.json đạt; `diff -qr`
vendor với npm cache nguyên bản `@casys/mcp-server@0.25.0` đạt. Kiểm lại sau
gates vẫn đạt. Frozen lock SHA-256 giữ nguyên:
`f32268af50c10ba06223c9a0b7f2d7092555ffa90172cd573ecf8d3feb2d882a`.

| Lệnh/gate                                               | Kết quả thực chạy                              |
| ------------------------------------------------------- | ---------------------------------------------- |
| Focused hai file với workaround frozen                  | 202 passed, 0 failed                           |
| `deno check mod.ts server.ts` với workaround frozen     | exit 0                                         |
| `deno lint --config deno.nojsr.json`                    | exit 0, 210 file                               |
| `deno fmt --check --ignore=.nojsr-vendor`               | exit 0, 262 file trước evidence                |
| `npm --prefix src/ui ci --offline --no-audit --no-fund` | exit 0, 208 package                            |
| `npm --prefix src/ui run typecheck`                     | exit 0                                         |
| `node src/ui/build-all.mjs`                             | exit 0, đủ 7 HTML                              |
| `npm_config_offline=true bash scripts/build-node.sh`    | exit 0                                         |
| `node --check dist-node/bin/hvgerp-mcp.mjs`             | exit 0                                         |
| `npm pack --dry-run --json --offline` tại dist-node/bin | exit 0, 10 file gồm 7 HTML, không testing host |
| `deno test --allow-all src/` với workaround frozen      | 1440 passed, 0 failed, 4 ignored               |
| `git diff --check` và staged diff check                 | exit 0; đã tự đọc toàn bộ diff source/test     |

Format check sau khi thêm evidence đạt 263 file. Full suite chạy sau khi UI/Node
build hoàn tất, không đồng thời UI build. Npm cảnh báo install-scripts cho
esbuild/fsevents chưa thuộc allowScripts; install/build vẫn exit 0, không đổi
policy hoặc dependency để che warning. Format loại riêng ignored vendor để bảo
toàn byte npm pristine; không claim đã format vendor hoặc đã chạy gate JSR mặc
định. Các bước UI task được chạy bằng lệnh Node/npm tương ứng để không resolve
config JSR mặc định.

Node bundle SHA-256:
`afb79ef9b5ce9ef138bd5b6b1db7785c6c286106f5c74449f32ae0780ce6c15e`. Artifact
build và workaround đều ignored, không commit.

## Giới hạn và bàn giao

Đây là bằng chứng local source/test, chưa phải CI JSR thật trên HEAD mới, kiểm
quyền/schema tại ERP production hoặc Browser. Không mở Browser/Vite, không
push/PR/merge và không tự ghi approval. Parent tiếp tục review độc lập, gate
hosted và cập nhật plan/index/manifest. Skill improve execute được dùng để khóa
phạm vi, chạy red/green thật và phân biệt gate đạt với gate chưa chạy.

## Review độc lập và tích hợp do parent xác nhận

Reviewer độc lập APPROVE HEAD `4c6da6770d1af10c7bb0a495b8a2ef8874d65116`, tree
`c67876790aa246a0314c9e6aa5e82a72162e67ba`, report blob
`2e9a6da65ed296326bcec129c86e0d2c44c48bf3`. Đã đọc đầy đủ plan/diff/evidence; tự
chạy boundary58, focused202, full1440/0/4, check, lint210, fmt263, UItype, Node
syntax và diff check. Reviewer không tự chạy lại red hoặc build; không suy kiểm
workaround thành native JSR. Parent đã đọc toàn bộ diff source, test và evidence
trước khi mở PR.

Parent merge main `ffcfee07b361f2d08fb1c58001cdbc3231707802` vào nhánh trong
commit `24b6597b4645e6d5be0107e75ba7c57a02e751ed`, tree
`97ae5b04fad4bbf7971dbaef77f2d9eb5b6b964e`. Merge sạch, chỉ thêm hai tài liệu
023; diff source/scripts/config so với bản được review rỗng. Full Deno frozen do
parent chạy lại đạt 1440 passed, 0 failed, 4 ignored. CI native JSR và Codex
trên HEAD PR là các gate tiếp theo, chưa được coi là đạt trong phần này.

## CI, Codex và merge do parent xác nhận

PR https://github.com/hvgllc/hvgerp-mcp/pull/43 đã merge lúc 12:11:23 UTC ngày
2026-09-05, commit `7d4546b8d303af5323205c75e6d6b51ba367d3c4`. Tree merge
`b2b014bc27c541b6b9aa6fe9c5c1c9cee3026420` bằng tree HEAD đã kiểm. CI
https://github.com/hvgllc/hvgerp-mcp/actions/runs/33964673608 thành công đúng
HEAD `c886e77a844b911eddac28e760fa6059cf08ea83`, dùng JSR thật. Log có tải
`@casys/mcp-server@0.25.0` từ jsr.io, 1440 passed, 0 failed, 4 ignored ở cả
Tests và release preflight không publish. Codex clean comment 5551639002 lúc
12:00:33 UTC ghi reviewed commit `c886e77a84`; parent đọc nguyên comment và xác
nhận fetch helper clean_comment=true, findings_error=false. Không release, đổi
version hoặc tác động ERP production.
